# ── vteam ──────────────────────────────────────────────────────────────────
# evidence: bản text commit, binary thì không
evd/**
!evd/**/
!evd/**/*.md
!evd/**/*.json
# snapshot doctor cục bộ cho `vteam board`
.vteam/doctor.json
# bytecode python do gate sinh lại — không commit, không manifest
.vteam/**/__pycache__/

# ── Bí mật ─────────────────────────────────────────────────────────────────
# token sống trong .env — .env KHÔNG bao giờ commit
.env
.env.*
!.env.example

# ── Tài liệu nội bộ ────────────────────────────────────────────────────────
# SRS/SRD phân loại "Nội bộ / Confidential" mà repo đang PUBLIC → giữ ở máy.
# HỆ QUẢ: khi BA trỏ specs.sources vào các file này, gate `verbatim` sẽ chạy
# được ở máy local nhưng ĐỎ trên CI (file không tồn tại ở đó). Hiện sources còn
# rỗng nên gate đang pass kèm cảnh báo. Đổi repo sang private để bỏ khối này.
docs/*.docx
docs/*.pdf
docs/*.html
# Q3 chốt 2026-08-19: giữ repo public, bản markdown nguồn của SRS/SRD CHỈ
# nằm ở máy. Không có dòng này thì docs/specs/sources/*.md sẽ được commit
# và toàn văn yêu cầu mật lên repo public — đúng thứ Q3 muốn tránh.
docs/specs/sources/

# ── Next.js / Node ─────────────────────────────────────────────────────────
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
/.next/
/out/
/build
/coverage
.vercel
*.tsbuildinfo
next-env.d.ts

# ── Prisma ─────────────────────────────────────────────────────────────────
# client sinh tự động bởi `prisma generate` (gate chạy trước typecheck/build)
/src/generated/

# ── Khác ───────────────────────────────────────────────────────────────────
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
