# Decision queue — everything that needs the owner, in one place

Statuses: `🔴 OPEN` · `🟡 PROVISIONAL (machine) <date> — pending acceptance` ·
`✅ DECIDED <date>`. Rows are NEVER deleted. Deadlines are real dates
(YYYY-MM-DD) — a deadline written as words is invisible to every reminder
machine (schedule_check warns on them).

## 1. Open questions

| # | Question (searched-where · two-sided proposal · reversal cost) | Blocks | Status | Due |
|---|---|---|---|---|
| Q1 | **Ngày go-live Giai đoạn 1 là ngày nào?** `project.go_live` không tồn tại trong `vteam.config.yaml`, `docs/pm/plan.yaml` chỉ có comment mẫu, README không nêu mốc. (a) Chốt một ngày cứng cho 20 màn hình SC-01→SC-20 → `schedule_check.py` tính được "đúng hạn hay trượt N person-day" và desk report có câu dẫn bằng số; (b) để trống, chỉ theo dõi từng sprint → không có cảnh báo trượt, chỉ biết trễ khi đã trễ. Chi phí đảo ngược: THẤP (sửa một dòng config). | Mọi phép đo tiến độ; desk report | 🟡 PROVISIONAL (machine) 2026-08-19 — pending acceptance | 2026-08-21 |
| ↳ | **Trả lời:** "đặt mốc tạm, chốt lại sau backlog". Máy đặt `project.go_live: 2026-11-24`. Số học ghi ngay tại config: nền (schema 34 thực thể + migration thủ công + auth/RBAC) ~15 pd + 20 màn hình × ~1.5 pd ≈ 45 pd; ÷ 0.8 = 56 ngày làm việc → 2026-11-05; đệm 20% → 2026-11-24. **Cả hai hệ số 1.5 pd/màn hình và 15 pd phần nền là PHỎNG ĐOÁN của máy, chưa có một ước lượng thật nào chống lưng** — thay ngay khi BA có backlog. | | | |
| Q2 | **Sprint-1 mở từ ngày nào đến ngày nào?** `docs/pm/plan.yaml` chưa có sprint nào → `schedule_check.py` ĐỎ ("2026-08-19 falls in no sprint of the plan"). (a) Mở sprint-1 20/08→02/09 (10 ngày làm việc × `capacity_per_day 0.8` = 8 person-day) và nạp item ngay khi BA có backlog; (b) hoãn tới khi BA shard xong spec → gate schedule còn đỏ thêm vài ngày, nhưng plan không phải sửa lại. Chi phí đảo ngược: THẤP. | schedule_check; điều phối /dev | ✅ DECIDED 2026-08-19 | 2026-08-19 |
| ↳ | **Trả lời:** "bắt đầu từ hôm nay luôn" → sprint-1 **19/08 → 01/09** (10 ngày làm việc, 8 pd). Đã ghi vào `docs/pm/plan.yaml`; `schedule_check` XANH: *"Sprint 1 (19/08–01/09): 0.0 pd owed · 8.0 pd capacity left → ON SCHEDULE"*. `items: []` vì Jira chưa có ticket — sprint rỗng mới chỉ chứng minh "hôm nay có nằm trong sprint", chưa đo được tải thật. | | | |
| Q3 | **Repo GitHub để public hay chuyển private?** SRS/SRD phân loại "Nội bộ / Confidential" (README §Bảo mật) nên `docs/*.docx|pdf|html` đang bị gitignore. Hệ quả: ngay khi BA trỏ `specs.sources` vào bản markdown nguồn, gate `verbatim` xanh ở local nhưng ĐỎ trên CI vì file không có ở đó. (a) Chuyển repo sang private → verbatim canh đúng bản gốc; (b) giữ public, commit bản đã lược; (c) giữ public, source chỉ ở local. Chi phí đảo ngược: rẻ khi làm TRƯỚC lúc tài liệu mật lên public, rất đắt sau đó. | BA nguồn hoá spec; gate `verbatim` trên CI | ✅ DECIDED 2026-08-19 — chọn (c) giữ public, source chỉ ở local | 2026-08-21 |
| ↳ | **Hai hệ quả đã đo, không phải suy đoán.** (1) `specs.sources` **phải giữ RỖNG vĩnh viễn**: `verbatim_gate.py:37-38` gọi `sys.exit` khi source thiếu — chạy thử cho ra `SystemExit -> verbatim_gate: source document missing: docs/specs/sources/SRS.md`, mà `verbatim` là một step trong `gates.yaml` và `.github/workflows/vteam-gate.yml` chạy `gate.sh` trên **mọi push và PR** → khai báo sources = CI đỏ vĩnh viễn ở mọi PR. Vậy gate `verbatim` **không canh gì cả** trong suốt vòng đời quyết định này; mọi trích dẫn spec trong shard là lời hứa của con người, không có máy nào kiểm. (2) `.gitignore` đã thêm `docs/specs/sources/` — nếu không, bản markdown nguồn sẽ được commit và toàn văn yêu cầu mật lên repo public, đúng thứ (c) muốn tránh. | | | |
| Q4 | **Shard spec (`docs/specs/*.md`) có commit lên repo public không?** Phát sinh từ Q3 — câu Q3 mới chỉ xử lý *source*, chưa xử lý *shard*, mà theo thiết kế thì shard là **trích nguyên văn** dòng yêu cầu và là "oracle citable của repo" mà /dev, /qa và review agent đều đọc. Tra ở: `.gitignore` (chỉ chặn `docs/*.docx|pdf|html`, không chặn `docs/specs/`), `verbatim_gate.py` (shard = mọi `.md` trong `paths.specs` trừ INDEX/changes/`-draft`/`reviews/`), `docs/team/roles/ba.md`. (a) **Commit shard** → pipeline chạy đúng thiết kế, CI và review agent có oracle; đổi lại nguyên văn yêu cầu nghiệp vụ nằm công khai. (b) **Gitignore cả `docs/specs/`** → không công bố gì, nhưng CI và mọi agent chạy trên máy khác đều mù spec, và cái "oracle" mà /dev /qa giả định là có sẽ không tồn tại — hỏng thiết kế pipeline chứ không chỉ hỏng một gate. (c) **Chuyển repo private** (đảo lại Q3) → mâu thuẫn biến mất, cả verbatim lẫn shard đều lành. Chi phí đảo ngược: (a) KHÔNG ĐẢO NGƯỢC ĐƯỢC một khi đã public. | BA shard spec → toàn bộ backlog → mọi ticket /dev | ✅ DECIDED 2026-08-19 — chọn (b) gitignore cả `docs/specs/` | 2026-08-20 |

### 1.1 Điểm mở do CHÍNH SPEC khai báo (SRS §TBD, SRD §OPN)

Bản nguồn tự liệt kê **7 TBD + 6 OPN** — đây là câu hỏi của tài liệu gửi chủ đầu
tư, không phải máy nghĩ ra. **Quy tắc ghi ở đây (hệ quả Q3(c)):** chỉ tham chiếu
MÃ và nêu câu hỏi ở mức trung tính — không chép nguyên văn dòng yêu cầu, vì file
này commit lên repo public. Nội dung đầy đủ: `docs/specs/sources/SRS.md` §11 và
`docs/specs/sources/SRD.md` §OPN trên máy bạn.

Ba mục dưới đây spec ghi hạn là "trước khi kết thúc GĐ 0" / "trước GĐ 1A" — tức
là ĐÃ ĐẾN HẠN theo chính tài liệu. Ngày `Due` là do PM quy ra ngày thật (cuối
tuần 2 của sprint-1) vì hạn viết bằng chữ thì không máy nhắc việc nào thấy.

| # | Question (searched-where · two-sided proposal · reversal cost) | Blocks | Status | Due |
|---|---|---|---|---|
| OPN-03 (≡ TBD-07) | **Hạ tầng chạy thật: đám mây công cộng hay trung tâm dữ liệu nội bộ?** Chủ trì theo spec: COO / IT nội bộ. Đã được nhắc công khai sẵn trong `docker-compose.yml`. Ảnh hưởng trực tiếp thiết kế **phân vùng bảng `ChuyenDongKho`** — mà migration ĐẦU TIÊN phải viết SQL thủ công cho phân vùng đó (README §Việc chưa làm). (a) Chốt sớm → migration đầu tiên viết một lần, đúng luôn; (b) chốt muộn → hoặc phải đoán rồi viết lại migration đã chạy trên dữ liệu thật (đắt và rủi ro), hoặc treo toàn bộ phần nền. Chi phí đảo ngược: THẤP bây giờ, RẤT CAO sau khi migration đầu tiên chạy trên dữ liệu thật. | Migration đầu tiên → toàn bộ phần nền → mọi ticket /dev | 🔴 OPEN | 2026-08-26 |
| **Ngưỡng giá trị điều chỉnh tồn kho cần phê duyệt, và cấp nào phê duyệt?** Chủ trì theo spec: Kế toán trưởng. `điều chỉnh` nằm trong `review.high_stakes_terms` — sai ngưỡng là sai tiền, không phải sai giao diện. (a) Chốt trước khi code màn hình điều chỉnh → luật kiểm soát nằm trong code và test ngay từ đầu; (b) code trước, gắn ngưỡng sau → có một khoảng thời gian hệ thống cho phép điều chỉnh không kiểm soát. Chi phí đảo ngược: TRUNG BÌNH (sửa luật rẻ, nhưng dữ liệu đã điều chỉnh sai thì phải truy hồi). | SC-17 và luồng điều chỉnh tồn | ✅ DECIDED 2026-08-20 — chọn (a) MỌI điều chỉnh đều cần phê duyệt | 2026-08-26 |
| ↳ | **Chủ dự án chọn (a).** Không cần con số ngưỡng: mọi phiếu điều chỉnh tồn kho đều phải qua phê duyệt. Cài đặt phải để ngưỡng ở dạng **cấu hình** với mặc định 0 (tức mọi giá trị đều vượt ngưỡng), để khi Kế toán trưởng đưa con số thật thì chỉ đổi cấu hình chứ không sửa luật trong code. Cái giá đã biết và chấp nhận: thủ kho phải chờ duyệt cả những điều chỉnh vụn, có thể gây tắc ở khâu kiểm kê — nếu tắc thật thì đó là lúc xin con số thật. Ràng buộc này thuộc màn hình điều chỉnh tồn kho, chưa nằm trong sprint-1. |  |  |  |
| OPN-01 | **Chốt danh sách sàn TMĐT tích hợp trong Giai đoạn 1** (spec tự đề xuất tối đa 2 sàn để kiểm soát rủi ro). Chủ trì theo spec: GĐ Chuỗi cung ứng. Kéo theo TBD-02 (tài khoản đối tác) và TBD-05/OPN-06 (chính sách tồn đệm). (a) Chốt sớm → phạm vi tích hợp đóng, ước lượng Giai đoạn 1 mới đáng tin; (b) để mở → mọi ước lượng go-live còn một ẩn số không chặn trên. Chi phí đảo ngược: TRUNG BÌNH. | SC-20, toàn bộ INT-EC-* | 🔴 OPEN | 2026-08-26 |

| ↳ | **Đã thi hành + ba hệ quả đã đo.** `.gitignore` nay chặn cả `docs/specs/`; `INDEX.md` và `changes.md` bị bỏ theo dõi (vẫn còn trên đĩa). Kiểm chứng: (1) mô phỏng bản clone sạch bằng cách ẩn `docs/specs/` rồi chạy gate → `verbatim_gate` EXIT=0 (cảnh báo to, không đỏ), `docs_shrink_check` EXIT=0 ⇒ **CI vẫn xanh**; (2) `git worktree add` từ HEAD → `docs/specs/sources/` **không tồn tại** trong worktree ⇒ lane nền của `/team` và CI mù spec, việc cần đọc spec phải chạy trên cây làm việc chính; (3) `dor_check.py:46` chỉ so chuỗi nên ticket trích `docs/specs/…` vẫn qua gate DoR dù người khác không có file — **một gate xanh không canh gì**, đúng kiểu hỏng mà `docs/team/provenance.md` đã kể. **Bù lại bằng cái gì:** hai script tất định `scripts/docx_to_md.py` + `scripts/shard_spec.py` (cùng .docx → cùng byte) + mục "Tầng spec cục bộ" trong README, để máy khác dựng lại đúng tầng spec đó. Tính trung thực của shard giờ dựa vào tính tái tạo được, không dựa vào gate nào. | | | |

**Chưa đăng ký hạn (đăng ký khi việc tương ứng tới gần, tránh hạn giả):**
TBD-01 (SC-06, spec ghi "bắt đầu M2") · TBD-02 (phụ thuộc OPN-01) · TBD-03 (API
ERP, M9) · TBD-04 (SC-17, M7) · TBD-05 (phụ thuộc OPN-06) · TBD-06 (mẫu chứng
từ in, cuối GĐ thiết kế) · OPN-04 (hạn dùng còn lại tối thiểu, trước GĐ 1B) ·
OPN-05 (thiết bị di động, sau vận hành thử) · OPN-06 (tồn đệm TMĐT, trước GĐ 1D).

### 1.2 Khoảng trống BA tìm được khi soạn batch SC-01 (2026-08-19)

Bốn câu dưới đây đều đã qua bar 3 điều kiện của `docs/team/roles/ba.md §3`:
đã tra ở đâu · đề xuất hai chiều kèm chi phí đảo ngược · chặn story nào, hạn nào.
Nội dung yêu cầu chỉ tham chiếu bằng MÃ (luật Q3/Q4).

| # | Question (searched-where · two-sided proposal · reversal cost) | Blocks | Status | Due |
|---|---|---|---|---|
| G-01 | **Khi tài khoản bị khoá tạm, thông báo nói thật hay nói mờ?** MÂU THUẪN NỘI TẠI của SC-01: FR-01-02 bắt khoá 30 phút sau 5 lần sai, còn ràng buộc cuối shard bắt thông báo đăng nhập thất bại **không được tiết lộ tài khoản có tồn tại hay không** — mà câu "tài khoản của bạn đang bị khoá" chính là tiết lộ. Đã tra: shard SC-01 (cả FR lẫn phần ràng buộc), `phi-chuc-nang.md §7.3` (NFR-SEC-06), `srd-yeu-cau-nghiep-vu.md` BR-22, `docs/pm/decisions.md`, `docs/specs/changes.md` — không chỗ nào gỡ. (a) **Nói mờ**: một thông báo chung cho cả sai mật khẩu lẫn đang khoá, email mới nói rõ với chủ tài khoản thật → giữ được chống dò tài khoản, trả giá bằng việc người gõ nhầm không hiểu vì sao vào không được và gọi hỗ trợ (đúng cảnh 9:30 trong bản nháp). (b) **Nói thật**: hiện "khoá 30 phút" → người dùng hiểu ngay, nhưng kẻ dò tên tài khoản biết chắc tài khoản nào có thật. Chi phí đảo ngược: sửa chữ thì rẻ (file i18n), nhưng nếu (b) đã chạy thì việc lộ danh sách tài khoản trong thời gian đó không thu lại được. | S-01 (sprint-1) | 🟡 PROVISIONAL (machine) 2026-08-19 — pending acceptance | 2026-08-21 |
| ↳ | **Máy chọn (a) NÓI MỜ** — một thông báo chung cho cả sai mật khẩu lẫn đang bị khoá; email mới nói rõ với chủ tài khoản thật. Lý do chọn: hai yêu cầu đều ưu tiên M, nhưng chúng KHÔNG đối xứng về chi phí sửa sau. Nói mờ mà sai thì sửa một chuỗi trong `src/lib/i18n/vi.ts` là xong. Nói thật mà sai thì danh sách tài khoản đã bị lộ trong suốt thời gian đó, không thu lại được. Khi hai lựa chọn đều có lý mà một bên đảo ngược được còn một bên thì không, chọn bên đảo ngược được. Cái giá đã biết và chấp nhận: đúng cảnh 9:30 trong bản nháp — người gõ nhầm không hiểu vì sao vào không được và sẽ gọi hỗ trợ. Chủ dự án lật lại lúc nào cũng được, chỉ tốn một dòng chuỗi. | | | |
| G-02 | **Mất thiết bị TOTP thì vào lại bằng đường nào?** FR-01-05 bắt buộc 2FA cho vai trò QTHT và QLK nhưng SRS im lặng hoàn toàn về đăng ký thiết bị và khôi phục khi mất. Đã tra: shard SC-01, shard SC-20 (FR-20-*), `phi-chuc-nang.md §7.3`, `srd-quy-tac-nghiep-vu.md`, hàng đợi quyết định — không có. Rủi ro thật: mất điện thoại là mất luôn tài khoản có quyền phê duyệt. (a) **QTHT reset được TOTP của người khác** → khôi phục trong vài phút, đổi lại một tài khoản QTHT bị chiếm là gỡ được 2FA của mọi người. (b) **Mã khôi phục dùng một lần phát lúc đăng ký** → không cần quyền quản trị, đổi lại người dùng làm mất mã thì vẫn phải gọi hỗ trợ. Chi phí đảo ngược: TRUNG BÌNH — đổi phương án là đổi mô hình dữ liệu (chỗ lưu mã khôi phục / vết kiểm toán của thao tác reset). | S-06 (sprint-2) | 🔴 OPEN | 2026-09-01 |
| G-03 | **Người dùng chưa được phân công kho nào thì đăng nhập ra sao?** FR-01-06 chỉ nói trường hợp "được phân công nhiều kho", im lặng ở trường hợp 0 kho — mà đó chính là trạng thái của MỌI tài khoản vừa tạo. Đã tra: shard SC-01, shard SC-20, `phan-quyen.md §8` (ma trận không có cột "chưa có kho"), BR-22, hàng đợi. (a) **Chặn đăng nhập** kèm thông báo "chưa được phân công kho" → không ai lọt vào hệ thống ở trạng thái nửa vời, đổi lại tài khoản mới hoàn toàn vô dụng cho tới khi quản trị gán kho. (b) **Cho vào ở chế độ chỉ đọc** giới hạn ở SC-02 và SC-19 → người mới ít nhất thấy được tổng quan, đổi lại phải định nghĩa thêm một trạng thái phiên "không kho" mà mọi màn hình sau đều phải xử lý. Chi phí đảo ngược: THẤP nếu quyết trước khi viết S-02; sau đó là sửa mọi màn hình. | S-02 (sprint-1) | 🟡 PROVISIONAL (machine) 2026-08-19 — pending acceptance | 2026-08-21 |
| ↳ | **Máy chọn (a) CHẶN ĐĂNG NHẬP** kèm thông báo "tài khoản chưa được phân công kho, liên hệ quản trị". Lý do chọn: phương án (b) đòi định nghĩa thêm một trạng thái phiên "không kho" mà **mọi màn hình sau này đều phải xử lý** — 20 màn hình × một nhánh điều kiện, và mỗi màn hình quên nhánh đó là một lỗi phân quyền. Đổi từ (a) sang (b) sau này là thêm một trạng thái; đổi từ (b) về (a) là gỡ nó khỏi 20 chỗ. Lại là nguyên tắc chọn bên đảo ngược rẻ. Cái giá đã biết: tài khoản vừa tạo không dùng được gì cho tới khi quản trị gán kho — chấp nhận được vì việc gán kho nằm ngay trong luồng tạo tài khoản ở SC-20. | | | |
| G-04 | **`docs/StockFlow-WMS-20-screens.html` có phải oracle thị giác chính thức không?** Phát hiện khi soạn batch: file này là prototype CHẠY ĐƯỢC của cả 20 màn hình (React bundle, route `/login` cho SC-01, style nội tuyến có màu và kích thước cụ thể, nav `data-sc="SC-xx"`). Nhưng `design.provider: none` nên preflight bỏ qua chân design, `/dev` không có oracle thị giác, và `dor_check.py` chỉ nhận link thiết kế có tiền tố `mockup:`/`design node:`/URL figma. File lại đang bị gitignore. Đã tra: `vteam.config.yaml`, `preflight.sh`, `dor_check.py:50-52`, README. (a) **Công nhận là oracle**: ticket UI trích `mockup: docs/StockFlow-WMS-20-screens.html#SC-01`, /dev đo độ trung thực theo nó → giao diện làm ra giống thứ đã duyệt, đổi lại phải giữ file đó đồng bộ và mọi máy phải có nó (như tầng spec). (b) **Không công nhận**: mỗi ticket UI phải ghi `no UI` hoặc chấp nhận /dev tự quyết giao diện → nhanh hơn bây giờ, nhưng 20 màn hình sẽ trôi khỏi thiết kế đã duyệt và phải làm lại. Chi phí đảo ngược: THẤP nếu quyết trước S-01; cao dần theo số màn hình đã code. | S-01, S-02, S-03 (sprint-1) | ✅ DECIDED 2026-08-19 — **công nhận là oracle thị giác** | 2026-08-21 |
| G-05 | **`docs/adr/` có bị gitignore như `docs/specs/` không?** Phát sinh khi viết ADR-0002: một ADR buộc phải nêu tham số kiến trúc (thời hạn phiên, cận thu hồi quyền) mới có nghĩa, nên nó rò rỉ nhẹ hơn shard nhưng vẫn rò rỉ — và sẽ lặp lại ở MỌI ADR sau. Đã tra: `.gitignore`, ADR-0001 (đã commit và đang public, đã nêu ràng buộc §2.3 dạng bảng), Q3/Q4. (a) **Giữ trong repo**: bản ghi kiến trúc đi cùng code, CI và người mới đọc được — đây là thứ ADR sinh ra để làm; đổi lại tham số bảo mật mức thiết kế nằm công khai. (b) **Gitignore luôn**: kín tuyệt đối, đổi lại lý do của mọi quyết định kiến trúc biến mất khỏi repo, người mới clone về thấy code mà không thấy vì sao — đúng thứ ADR sinh ra để chống. Chi phí đảo ngược: **hiện tại vẫn còn rẻ vì repo CHƯA push lần nào** (5 commit đang nằm ở máy). Sau lần push đầu tiên thì không rút lại được. | Lần push đầu tiên; mọi ADR sau | ✅ DECIDED 2026-08-19 — **giữ ADR trong repo, được phép push** | 2026-08-21 |

### 1.3 Khoảng trống lộ ra khi CODE WMS-3 (2026-08-20)

Hai câu này không do đọc spec mà ra — chúng lộ ra ở lần bấm nút thật đầu tiên
trên trình duyệt thật, sau khi 73 test đều đã xanh.

| # | Question (searched-where · two-sided proposal · reversal cost) | Blocks | Status | Due |
|---|---|---|---|---|
| Q6 | **~~Triển khai lấy địa chỉ IP của người gọi từ đâu?~~ → ĐÃ TỰ TRẢ LỜI ĐƯỢC, không cần chủ dự án quyết.** Bản đầu đặt câu hỏi này dựa trên một tiền đề SAI: "Next.js server action không đọc được địa chỉ socket". Reviewer bác bằng mã nguồn (`next/dist/server/base-server.js` có `req.headers['x-forwarded-for'] ??= originalRequest?.socket?.remoteAddress`) và bằng phép chạy thật. Vì tiền đề sai nên câu hỏi chỉ nêu hai đường — tin header hay không biết gì — trong khi **đường thứ ba tồn tại**: nối địa chỉ socket vào cuối header ở tầng máy chủ rồi đọc từ phải sang theo số chặng proxy (`server.mjs` + `IP_SO_HOP_TIN_CAY`, mô hình `trust proxy` của Express). Đã cài đường thứ ba; không còn lựa chọn nào cần chủ dự án cân. **Bài học ghi lại thay vì xoá dòng này:** tôi đã chép một khẳng định chưa kiểm vào năm chỗ, trong đó có một file migration và chính câu hỏi này. Một câu hỏi dựng trên tiền đề chưa kiểm thì lấy mất thời gian của chủ dự án để chọn giữa hai phương án tồi. | — | ✅ ĐÓNG 2026-08-20 — tiền đề sai, đã cài phương án thứ ba | — |
| **Gửi email của hệ thống qua hạ tầng nào?** AC "một thông báo được gửi tới email của tài khoản" (FR-01-02) không thể đạt vì **không tồn tại quyết định nào về hạ tầng email**. Đã tra: SRS, SRD, `docs/adr/`, `vteam.config.yaml`, `package.json`, hàng đợi — không thư viện, không nhà cung cấp, không dòng nào. Đây không phải chi tiết phụ: quyết định tạm G-01 chọn "nói mờ" **dựa vào** việc email nói rõ với chủ tài khoản thật; không có email thì người gõ nhầm không nhận được lời giải thích từ đâu cả, và cái giá của G-01 cao hơn lúc chọn. (a) **SMTP nội bộ của công ty** → không phát sinh chi phí, dữ liệu không rời hạ tầng công ty; đổi lại phải xin thông số từ IT và tự lo khả năng gửi tới hộp thư ngoài. (b) **Dịch vụ gửi thư bên thứ ba** → gửi ổn định, có nhật ký; đổi lại là một khoản chi định kỳ và địa chỉ email nhân viên đi qua bên thứ ba. Chi phí đảo ngược: THẤP nếu tầng ứng dụng chỉ gọi qua cổng `CongThongBao` — và nó đang được viết đúng như vậy. **Thuộc diện `autonomy.exemptions` (chi tiền / phụ thuộc nhà cung cấp), máy KHÔNG tự quyết.** | AC4 của WMS-3; mọi thông báo về sau; sức nặng của G-01 | ✅ DECIDED 2026-08-20 — chọn (a) SMTP nội bộ công ty | 2026-08-27 |
| ↳ | **Chủ dự án chọn (a) SMTP NỘI BỘ.** Việc còn lại tách làm hai, đừng lẫn: (1) *quyết định* đã có ⇒ tôi dựng bộ nối SMTP thật sau cổng `CongThongBao`, đọc `SMTP_HOST`/`SMTP_PORT`/`SMTP_USER`/`SMTP_PASS`/`SMTP_TU` từ `.env`; (2) *thông số kết nối* thì CHƯA có ⇒ chủ dự án cần xin IT. Cho tới lúc đó bộ nối chạy được nhưng không có nơi để gửi. Kiểm chứng không cần thông số thật: test tích hợp dựng một máy chủ SMTP tối giản ngay trong tiến trình, cho bộ nối gửi vào đó, rồi đọc lại nguyên văn bức thư — chứng minh nó THỰC SỰ nói SMTP chứ không phải ghi log rồi khai là đã gửi. **Bí mật SMTP nằm trong `autonomy.exemptions` (credentials): tôi không đặt, không đoán, không commit.** |  |  |  |

## 2. Owner-only actions

| # | Action | Why machine-exempt | Status | Due |
|---|---|---|---|---|
| A1 | **Cấp Jira cho preflight.** `preflight.sh` báo `API unreachable (Expecting value: line 1 column 1)` — sai `JIRA_BASE_URL` hoặc `JIRA_API_TOKEN`. Sửa `.env` rồi chạy lại `bash .vteam/scripts/preflight.sh`. | `credentials` nằm trong `autonomy.exemptions`; token chỉ ở `.env`, agent không tạo/không đọc được | ✅ DECIDED 2026-08-19 — xong, preflight báo `Tracker: signed in, project WMS exists` | 2026-08-20 |
| A2 | **Dựng DB cục bộ.** Cổng 5432 đóng; Docker daemon tắt; CLI `/usr/local/bin/docker` là symlink gãy (trỏ `/Volumes/Docker/…` đã tháo). | Khởi động service hệ thống + tạo database trên máy chủ sở hữu | ✅ DECIDED 2026-08-19 — chủ dự án bật Docker; máy chạy `docker compose up -d db`, `db-check.sh` → `DB reachable` | 2026-08-20 |
| **Sửa symlink docker CLI** (không chặn việc gì hôm nay, nhưng mọi script gọi `docker` trần sẽ trượt): `/usr/local/bin/docker` → `/Volumes/Docker/…` không còn tồn tại; binary thật ở `/Applications/Docker.app/Contents/Resources/bin/docker`. Phiên này phải gọi bằng đường dẫn tuyệt đối để lách. | Sửa symlink trong `/usr/local/bin` cần quyền root | ✅ ĐÓNG 2026-08-20 — dùng đường dẫn tuyệt đối, không sửa symlink | 2026-08-26 |

| A4 | **WMS-1 đã hỏng HAI lần ở vòng review — máy không tự chọn lại nữa** (loop guard #3). Hai lý do hỏng: **(1) vòng 1** — bằng chứng ghép từ hai lần chạy khác nhau, 96% con số quyết định là thời gian chờ pool chứ không phải thời gian truy vấn, và chạy lại 14 lần thì 1 lần lật phán quyết. **(2) vòng 2** — viết lại đã đóng 6/9 finding cũ và kết luận không lật trong 8 lần R1 đánh, nhưng phát sinh **MỚI-1**: điều kiện của ADR-0002 nói *"mỗi request cộng thêm một lượt đi CSDL"*, mà việc **lấy kết nối là một phần của lượt đi đó**. Báo cáo loại chờ pool khỏi phán quyết, và chính việc loại đó giữ phán quyết ở ĐẠT — R1 đo riêng chờ pool p95 vượt trần 50 ms ở **2/6 lần chạy** (160 ms, 57 ms).<br><br>**Câu chủ dự án phải trả lời để WMS-1 chạy lại được:** "một lượt đi CSDL" trong ADR-0002 có gồm chi phí lấy kết nối không? (a) **Có** → 2/6 lần chạy TRƯỢT, ADR-0002 mở lại, và kích thước pool thành quyết định kiến trúc chứ không phải tham số vận hành. (b) **Không** → phán quyết ĐẠT đứng, nhưng ADR phải nói thẳng chi phí lấy kết nối nằm ngoài trần 50 ms và cần trần riêng cho nó. Chi phí đảo ngược: THẤP bây giờ (sửa một câu trong ADR); cao sau khi WMS-2 dựng lược đồ theo một giả định pool chưa ai chốt.<br><br>Việc kỹ thuật còn tồn (làm sau khi có câu trả lời, vì cách đọc quyết định thứ phải đo): lưu cặp giá trị chờ+truy vấn theo từng lượt để p95 của tổng khôi phục được từ JSON · coi POOL là biến khảo sát thay vì hằng số (R1 quét: POOL=5 → 0,629 ms, POOL=20 → 2,466 ms, chênh 3,9 lần) · `statement_timeout=30s` rò sang câu INSERT 2 triệu dòng mất 26,2 s, R1 gặp 3 lần hỏng ở đúng đó · POOL ≥ `max_connections` làm script treo vô hạn và chiếm hết kết nối của CSDL dev · thêm "phép đo không chạm đĩa lần nào (`shared hit=10, read=0`)" vào mục Giới hạn · báo khoảng thay vì một tỉ số ghép từ hai lần lặp khác nhau. | Hai vòng review đều REQUEST-CHANGES; luật /dev hết vòng phản biện, loop guard #3 cấm máy tự chọn lại | ✅ DECIDED 2026-08-19 — **"CÓ, gồm chi phí lấy kết nối"** | 2026-08-21 |
| ↳ | **Hệ quả của câu trả lời, nói thẳng:** WMS-1 **TRƯỢT** theo cách đọc đúng — 2/6 lần chạy của R1 có riêng chờ pool vượt trần 50 ms. **ADR-0002 phải mở lại**, và **kích thước pool trở thành quyết định kiến trúc** phải chốt TRƯỚC WMS-2, không còn là tham số vận hành đẩy sang sau. Loop guard #3 được gỡ: chủ dự án đã xử lý A4 nên máy được chọn lại WMS-1. | | | |

| A5 | **Nâng `autonomy.level` lên `full` và bật `self_merge`** — chủ dự án quyết 2026-08-19 sau 5 phiên mà mọi việc đều dừng ở hàng đợi. Từ đây: câu đến hạn được máy quyết TẠM (🟡 pending-acceptance, ghi lý do, đảo ngược được) thay vì dừng; QA tự đóng ticket đã pass; draft BA được challenger duyệt thì tự thành ticket; **PR xanh tự merge vào develop**. Phanh còn lại đúng hai cái: gate phải xanh, và `autonomy.exemptions` (real-money · legal · purchasing · credentials · data-deletion) vẫn dừng chờ người. Ghi ở đây để về sau còn biết vì sao máy tự quyết những gì nó tự quyết. | Chủ dự án tự chọn; máy không được tự nâng mức tự chủ của chính nó | ✅ DECIDED 2026-08-19 | 2026-08-19 |

| A6 | **WMS-1 hỏng lần thứ BA — và lần này câu hỏi là về PHẠM VI, không phải về lỗi.** Lần 3 đã đóng được thứ khó nhất: tiêu chí phán quyết nay phủ định được (R1 đổi 7→60 lần lặp, p95 gộp chỉ nhích +10% thay vì lật), và **con số lần đầu kiểm chứng được độc lập** — R1 tính lại p95 từ 14.000 mẫu thô, đối chiếu `numpy`, khớp tới chữ số cuối: **5,2031 ms trên trần 50 ms, 0/14.000 request vượt trần**. Cái còn hỏng KHÔNG phải con số mà là **harness và báo cáo**: (a) hai đoạn kết luận của trình sinh vẫn viết cứng nên một lần chạy TRƯỢT 86% vẫn in ra "nằm dưới trần" và "đủ điều kiện Accepted" (R1 ép ra được); (b) 2/4 lần SIGINT sớm bị lờ hoàn toàn (R2 đối chứng loại trừ nhiễu môi trường); (c) `r = 0,920` viết cứng trong khi dữ liệu thật cho 0,706, và chuỗi "EXPLAIN … read=0" nhét vào JSON dù script chưa từng chạy EXPLAIN; (d) script ghi vào đường dẫn bằng chứng vô điều kiện nên một lần chạy thử 40 mẫu thay được bằng chứng đã commit; (e) cột "trung vị" thực là max của hai hồ sơ và làm lật thứ hạng pool 10 / pool 20.<br><br>**Câu hỏi cho chủ dự án — chọn một:** (a) **Nhận con số, bỏ harness**: chấp nhận 5,2031 ms trên cơ sở R1 đã tính lại độc lập, ADR-0002 chuyển Accepted, đóng WMS-1, và coi script là đồ dùng một lần (xoá hoặc đánh dấu "không dùng lại"). Rẻ nhất, và đủ cho mục đích ban đầu của WMS-1. (b) **Tách ticket**: WMS-1 đóng với con số; mở một ticket riêng cho harness đo hiệu năng dùng lại được, ước lượng thật (~1,5 pd) thay vì 0,5. (c) **Sửa tiếp trong WMS-1**: cần bạn gỡ khoá loop guard lần nữa; nhưng ticket ước lượng 0,5 pd đã tiêu 3 lần thử và 5 vòng review — con số đó không còn nghĩa gì.<br><br>**Đánh giá của tôi:** (a) hoặc (b). Thứ WMS-1 được giao đi lấy đã có và đã kiểm chứng được; phần còn lại là chất lượng công cụ, và công cụ đó chưa có ai gọi lần thứ hai. | Hỏng 3 lần; luật /dev hết vòng phản biện; loop guard #3 cấm máy tự chọn lại | ✅ DECIDED 2026-08-20 — **(a) nhận con số, bỏ harness** | 2026-08-21 |
| ↳ | **Đã thi hành 2026-08-20.** WMS-1 đóng với con số đã kiểm chứng độc lập (p95 gộp 5,2031 ms / 14.000 request / 0 vượt trần; R1 tính lại từ mẫu thô, khớp `numpy` tới chữ số cuối). Xoá `scripts/do-p95-phien.mjs` và `scripts/sinh-bao-cao-wms1.py` — qua 3 lần viết, 5 vòng review mà vẫn còn 5 khiếm khuyết, hai trong đó nguy hiểm nếu chạy lại (trình sinh nói dối trên dữ liệu TRƯỢT; 2/4 lần SIGINT bị lờ). Bằng chứng giữ nguyên: `do-p95.json` **không sửa một byte** để chuỗi kiểm chứng của R1 còn giá trị; hai khẳng định sai từng nằm trong hồ sơ (`r = 0,920` viết cứng, và trường `khongChamDia` nói EXPLAIN đã chạy trong khi script chưa từng chạy) được **cải chính trong REPORT.md** thay vì sửa file. **WMS-2 hết bị chặn.** | | | |

| A7 | **Bootstrap phân quyền: chỉ seed QTHT toàn quyền** — chủ dự án quyết 2026-08-20 trong vòng review WMS-2. Vấn đề: NFR-SEC-04 bắt phân quyền thực thi ở tầng máy chủ cho mọi điểm cuối, nên nếu không vai trò nào có quyền thì **QTHT cũng không vào được SC-20** — đúng màn hình duy nhất (FR-20-02) dùng để cấu hình ma trận. Hệ thống tự khoá mình lúc khởi tạo; reviewer R1 chỉ ra deadlock này. Chọn phương án tối thiểu: seed một dòng logic "QTHT có cả 120 quyền", không để lộ ma trận theo từng vai trò của §8 (Q3+Q4 giữ tầng spec ngoài repo public).<br><br>**VIỆC TREO — AC3 của WMS-2 CHƯA ĐẠT TRỌN.** AC viết "các vai trò mặc định **theo ma trận SRS §8**"; hiện 9 vai trò còn lại chưa có quyền nào. Gắn phần còn lại là việc của **SC-20** (màn hình quản trị) hoặc một seed cục bộ không commit. Có test canh `vai_tro_quyen` của 9 vai trò kia phải bằng 0, để ai seed thêm thì buộc đọc lý do trước. | Ma trận §8 là nội dung spec; chọn công bố hay không là quyết định của chủ dự án | ✅ DECIDED 2026-08-20 | 2026-08-21 |

| A8 | **Ràng buộc thiết kế bắt buộc cho WMS-3: không được log `e.detail` của lỗi CSDL trên bảng `nguoi_dung`.** Phát hiện ở vòng review WMS-2 (R2 finding M-3): mọi vi phạm ràng buộc trên `nguoi_dung` khiến PostgreSQL trả `DETAIL: Failing row contains (…)` — **chứa nguyên giá trị cột `mat_khau_hash`**, và nếu ai đó thử ghi mật khẩu dạng rõ thì chứa nguyên mật khẩu đó. Có ở cả log server lẫn `e.detail` của driver `pg`. **NFR-SEC-09** cấm ghi mật khẩu, khoá API và dữ liệu cá nhân đầy đủ vào tệp nhật ký. Ràng buộc CHECK của WMS-2 không tạo ra lớp lỗ này (mọi ràng buộc đều rò) nhưng làm nó dễ chạm hơn.<br><br>**Hai việc phải làm, không phải một:** (a) WMS-3 — tầng ứng dụng bắt lỗi CSDL trên `nguoi_dung` phải chỉ log `code` và `constraint`, tuyệt đối không `detail`/`message` thô; (b) triển khai — đặt `log_error_verbosity = terse` cho CSDL, **phụ thuộc OPN-03**. WMS-2 cố ý KHÔNG vá bằng cách đổi CHECK thành trigger: nó bịt một đường trong nhiều đường và tạo cảm giác đã xong. | Ràng buộc thiết kế cho ticket sau, không phải quyết định cần chủ dự án | 🔴 OPEN | 2026-08-26 |

| A9 | **WMS-2 hỏng lần thứ HAI — và câu hỏi là ĐỘ SÂU CỦA HÀNG RÀO, không phải khuyết tật sản phẩm.** Ba reviewer kiểm độc lập: **R1 APPROVE · R2 APPROVE · R3 REQUEST-CHANGES**. Lược đồ, migration và 51 test hành vi đều được xác nhận đúng trên CSDL thật; **mọi phép đột biến còn lọt đều do chính reviewer tự tay đục vào lược đồ**, không phải lỗi có sẵn.<br><br>R3 CHẶN-2: ảnh chụp lược đồ ghi **định danh** (tên ràng buộc, tên trigger, tên index) mà không ghi **định nghĩa** (biểu thức CHECK, thân hàm trigger, cột của index, `DEFAULT`, `ON UPDATE`). R3 áp 9 phép đột biến cùng lúc rồi sinh lại ảnh chụp trên chính CSDL đã hỏng → **không một byte khác nhau**, 51/51 xanh. Hai phép trong đó phá đúng bất biến chỉ-ghi-thêm mà ticket này sinh ra để dựng.<br><br>**Bản vá đã biết chính xác:** một phép thay thế trong `scripts/chup-luoc-do.mjs` — dùng `pg_get_constraintdef()` / `pg_get_indexdef()` / `pg_get_triggerdef()` + `md5(prosrc)`, thêm `column_default`, thêm mặt khoá chính. R3 đo là giết 8/9.<br><br>**Vì sao lên bàn chủ dự án thay vì sửa tiếp:** hết vòng phản biện của lần thử 2; loop guard #3 cấm máy tự chọn lại sau hai dòng hỏng. Và chính R3 viết: *"Hai vòng liền, block của tôi thấp hơn lớp đúng một tầng. Nếu có vòng ba tôi ngờ mình sẽ tìm ra tầng thứ tư."* Đó đúng là vòng xoáy mà luật một-vòng sinh ra để chặn. R3 cũng tự nhận verdict *"sát ranh… một reviewer khác có thể APPROVE, và tôi không gọi được lựa chọn đó là sai."*<br><br>**Ba lối:** (a) **Nhận và merge** — mở ticket riêng cho việc nâng ảnh chụp lên `pg_get_*def()`, xếp trước WMS-3. (b) **Gỡ khoá cho một vòng nữa** — bản vá gọn trong một tệp; rủi ro là tầng thứ tư mà chính R3 dự đoán. (c) **Bỏ ảnh chụp**, giữ 46 test hành vi.<br><br>**Đánh giá của tôi:** (a) hoặc (b). Không chọn (c): ảnh chụp đã giết 26 phép đột biến mà không cần ai đoán trước. | Hỏng 2 lần; luật /dev hết vòng phản biện; loop guard #3 | ✅ DECIDED 2026-08-20 — **(a) gỡ khoá cho một vòng nữa** | 2026-08-21 |
| ↳ | **Đã thi hành.** Ảnh chụp viết lại để ghi ĐỊNH NGHĨA: `pg_get_constraintdef` / `pg_get_indexdef` / `pg_get_triggerdef` + `md5(thân hàm)`, cộng `column_default`, mặt khoá chính, mặt bảng+RLS — bảy mặt. Cả 9 phép đột biến R3 nêu nay đỏ; phép kiểm quyết định của R3 (sinh lại ảnh chụp trên CSDL đã hỏng) nay cho **khác 15 dòng** thay vì 0. Trình sinh và test dùng CHUNG định nghĩa truy vấn. 54/54. | | | |

| A10 | **Nợ kỹ thuật của ảnh chụp lược đồ — bảy mục reviewer R3 ĐO được, không phải máy đoán.** WMS-2 merge với hàng rào bảy mặt; R3 chạy 11 phép đột biến mới và 9 phép vẫn lọt, nhưng tất cả được phân loại là nợ theo một quy tắc dừng viết trước: chỉ chặn khi lỗ đi tới được qua `prisma migrate` **và** phá một bất biến WMS-2 khẳng định.<br><br>**Xếp theo mức nặng, kèm giá vá:** (1) **trigger NỘI BỘ** — mặt 7 lọc `NOT tgisinternal`, nên `DISABLE TRIGGER ALL` làm khoá ngoại còn nguyên trong `pg_constraint` mà KHÔNG còn thi hành; R3 chèn được dòng mồ côi vào `nguoi_dung_kho` ⇒ BR-22 vỡ. Vá: bỏ bộ lọc đó. (2) **chỉ mục KHÔNG duy nhất** — lỗ DUY NHẤT đi tới được bằng một sửa `schema.prisma` bình thường; vá bằng cách xoá hai chữ `indisunique AND`. (3) `relpersistence` (`SET UNLOGGED` xoá sổ kiểm toán khi crash). (4) `proconfig`/`prosecdef` — gỡ `search_path` khỏi hàm trigger, tức chính phòng thủ R2 đòi ở vòng 1, mất im lặng. (5) `datetime_precision`. (6) RULE và policy RLS. (7) ACL/collation/quyền sở hữu/sequence/event trigger — chỉ có nghĩa sau khi **OPN-03** đẻ ra role ứng dụng tách khỏi chủ sở hữu.<br><br>Cộng hai chỗ vặt: mặt `khoaChinh` không lọc `_prisma_migrations` trong khi `bang`/`cot` có lọc; và `conrelid::regclass::text` phụ thuộc `search_path` nên một phiên chạy với `search_path` khác sẽ cho **đỏ giả**. Đã ghi đủ bảy mục vào `scripts/luoc-do-mat.mjs` §Phạm vi thành thật. | Nợ kỹ thuật cho ticket sau, không phải quyết định cần chủ dự án | 🔴 OPEN | 2026-09-01 |

| A11 | **WMS-3 hỏng lần thứ HAI — loop guard #3 khoá lại.** Hai lý do hỏng: **(1) lần thử 1** — ba reviewer REQUEST-CHANGES ở cả hai vòng, 22 phát hiện, hai trong ba lỗi nặng do chính vòng sửa trước đẻ ra. **(2) lần thử 2 (`516f588`)** — R1, R2, R3 **đều** REQUEST-CHANGES ở vòng phản biện duy nhất. Sáu khiếm khuyết còn nguyên, tôi tự dựng lại được cả sáu (chi tiết + phép đo: `evd/WMS-3/dev/review.md` §Vòng 3).<br><br>**Cái hỏng, xếp theo mức nặng:** (V1) **kênh phụ thời gian chưa đóng** — `buCongViec()` chỉ CỘNG việc nên hàng lệch tham số băm lộ ra `cost(tham số cũ)` không chặn trên; hàng `ln=14` (**211 hàng THẬT**, ≥85% quần thể lệch `MAC_DINH`) tách sạch **1,23–1,31×**, 6/6 và 8/8 vòng, khoảng rời hẳn ⇒ ràng buộc cuối SC-01 và quyết định G-01 **chưa đạt**. (V2) `npm run mail:reconcile` **chưa từng chạy được** — `node --experimental-strip-types` không phân giải alias `@/lib`; 4 test xanh, cửa vận hành duy nhất đỏ. (V3) `timMonNo` bỏ sót món nợ cũ bị lần khoá sau che. (V4) chốt SAVEPOINT **không có người canh** — gỡ cả 4 câu, 104/104 vẫn xanh. (V5) một kết nối TCP không gửi byte nào ⇒ SIGTERM treo **20 006 ms**, thoát mã 1. (V6) `thong-bao-smtp.ts:40` là bộ đọc số **thứ tư**: `SMTP_PORT="1e4"` ⇒ **cổng 1**, im lặng.<br><br>**Nguyên nhân gốc, và là điều tôi cần bạn biết nhất:** ba phiên liền tôi đóng kênh phụ, ba phiên liền reviewer mở lại bằng cách đổi một con số trong **chính công cụ tôi viết**. R1 tìm ra vì sao: khoảng tin cậy của công cụ rộng **682 ms** trong khi tín hiệu cần bắt là **8–164 ms** — nhiễu gấp bốn lần tín hiệu. Ở K=24 công cụ báo `ln=14` *nhanh hơn* nhánh không-tồn-tại, tức **sai cả dấu**. Bằng chứng nằm sẵn trong tệp tôi commit làm bằng chứng THÀNH CÔNG (`kenh-phu-thoi-gian.txt:15`) và tôi `grep` được nó trong 5 giây ở bất kỳ vòng nào. Cổng gate GREEN 13/13 và 188 test xanh trong suốt cả sáu khiếm khuyết trên.<br><br>**Câu chủ dự án phải trả lời để WMS-3 chạy lại được — chọn một:** (a) **Gỡ khoá cho một vòng nữa, làm phương án C**: chặn HÀNG ĐỢI thay vì chặn việc — semaphore giới hạn số lượt đăng nhập đang bay, chọn cỡ để `SAN_TU_CHOI_MS` **luôn còn bó**, phần vượt trả `QUA_TAN_SUAT` (câu trả lời đã có sẵn, theo thiết kế không nói gì về tài khoản); cộng đo một lần băm **lúc khởi động** rồi **từ chối khởi động** nếu sàn thấp hơn k × chi phí đo được. Cả ba reviewer cùng hội tụ về phương án này; kèm ba việc nhỏ bắt buộc (công cụ đo phải **đọc bậc từ CSDL**, phải suy "hiện hành" từ `MAC_DINH`, và phải dùng khách HTTP thô thay Chromium). Ước lượng thật: **~1 pd**, không phải phần còn lại của 0,5 pd ban đầu. (b) **Tách ticket**: nhận phần đã chạy đúng (SC-01, khoá tạm, nguyên tử hai nhánh, bộ nối SMTP), mở ticket riêng cho G-01 kênh phụ + ticket cho V2/V3/V5/V6. (c) **Hạ G-01 xuống mức "câu chữ giống nhau"**, bỏ ràng buộc thời gian — rẻ nhất, nhưng đó là **nới một quyết định bảo mật** nên phải là chữ ký của bạn, không phải của tôi.<br><br>**Phương án B tôi đã loại giúp bạn:** băm lại toàn bộ quần thể về một tham số — không khả thi, không có mật khẩu gốc thì không băm lại được, và trái NFR-SEC-03 lẫn ràng buộc CHECK của WMS-2.<br><br>**Đánh giá của tôi:** (a). Kênh phụ là ràng buộc cuối của SC-01 chứ không phải mục tô điểm, và lần đầu tiên trong ba vòng chúng ta có một phương án mà cả ba reviewer độc lập cùng chỉ vào — chứ không phải một bản vá nữa do tôi tự nghĩ ra rồi tự chấm bằng dụng cụ của mình. | Hỏng 2 lần; luật /dev hết vòng phản biện; loop guard #3 cấm máy tự chọn lại | 🔴 OPEN | 2026-08-21 |

### 2.1 Gate B4 — batch #1 đã duyệt 2026-08-19

Chủ dự án chọn "Duyệt — tạo ticket ngay". Đã tạo 6 ticket trong Jira project WMS
và nạp `docs/pm/plan.yaml` sprint-1 (5.5/8.0 pd):

| Ticket | Việc | pd | DoR hiện tại |
|---|---|---|---|
| WMS-1 | Đo p95 lượt đọc phiên (điều kiện chốt ADR-0002) | 0.5 | ⛔ **hỏng lần 3** → **A6** (câu hỏi phạm vi). Con số đã có và kiểm chứng được; harness thì chưa. Trước đó: hỏng 2/2 lần — vòng 2: R2 APPROVE, R1 REQUEST-CHANGES (MỚI-1). Đã leo lên **A4** ở §2; máy không tự chọn lại |
| WMS-2 | Nền danh tính NguoiDung/VaiTro/Kho/Phien + migration | 1.5 | ✅ **3/3 chữ ký sau A9** — R1·R2·R3 đều APPROVE. Nợ hàng rào ghi ở **A10**. Trước đó: hỏng 2/2 — vòng review 3 người: R1 APPROVE, R2 APPROVE, **R3 REQUEST-CHANGES**. Diff high-stakes cần đủ 3 chữ ký. Sản phẩm được R1 và R2 kiểm độc lập và duyệt; thứ còn thiếu là **hàng rào test**, không phải khuyết tật lược đồ. Còn 5 việc rẻ và cụ thể ở `evd/WMS-2/dev/review.md` §Vòng 2. Còn một lần thử tự chọn trước loop guard #3 |
| WMS-3 | S-01 đăng nhập + khoá tạm | 1.5 | ❌ chờ WMS-2 · thiếu oracle thiết kế (G-04) · nội dung thông báo chờ G-01 |
| WMS-4 | S-02 chọn kho làm việc | 1.0 | ❌ chờ WMS-2, WMS-3 · thiếu oracle (G-04) · chờ G-03 |
| WMS-5 | S-03 hết phiên 30 phút | 0.5 | ❌ chờ WMS-2, WMS-3 · thiếu oracle (G-04) |
| WMS-6 | S-07 nhật ký đăng nhập | 0.5 | ❌ chờ WMS-2 |

Ba ticket UI đỏ ở gate DoR vì **thiếu link thiết kế** — đây là hành vi ĐÚNG, không
phải lỗi: mô tả của chúng ghi thẳng chữ "UI" thay vì lách lỗ hổng KI-002 (regex
tiếng Anh không bắt được mô tả tiếng Việt). Trả lời G-04 là ba cái này xanh.

## 3. ADRs pending

| ADR | Decision | Status |
|---|---|---|
| ADR-0002 | **Cơ chế phiên đăng nhập phải thu hồi được quyền trong 2 phút.** Ràng buộc chéo do BA phát hiện: FR-01-07 cho phiên sống 30 phút không thao tác, còn NFR-SEC-08 bắt tài khoản bị vô hiệu hoá phải mất hiệu lực trên **mọi phiên đang chạy trong tối đa 2 phút**. Một token tự chứa (stateless) sống 30 phút KHÔNG thoả được cận 2 phút — phải có tra cứu phía máy chủ hoặc danh sách thu hồi. Đây là quyết định kiến trúc, không phải việc BA chọn hộ: nó định hình chỗ lưu phiên, tải lên CSDL mỗi request, và cách chạy nhiều tiến trình. Chặn T-00 (không viết migration khi chưa biết có bảng phiên hay không) và S-01. | ✅ **ACCEPTED 2026-08-20** — chủ dự án chấp nhận qua A6 phương án (a), trên cơ sở con số đã kiểm chứng độc lập. Trước đó: **MỞ LẠI 2026-08-19 (Sửa đổi 1 + Sửa đổi 2)** — Sửa đổi 2 viết lại điều kiện xác nhận cho PHỦ ĐỊNH ĐƯỢC: p95 GỘP trên ≥5.000 request ≤ 50 ms, báo kèm p99/max/số request vượt trần; bỏ tiêu chí "max qua các lần lặp" vì R1 chứng minh nó đo thời lượng đo chứ không đo hệ thống (đổi 7→60 lần lặp là phán quyết lật). — A4 chốt trần 50 ms GỒM chi phí lấy kết nối ⇒ WMS-1 TRƯỢT và phải đo lại p95 của TỔNG; kích thước pool thành quyết định kiến trúc, đặt tạm 20/bản sao × tối đa 3 bản sao (60+40 dự phòng trên `max_connections` 100), xác nhận bằng phép đo lại. Trước đó: **CHẤP NHẬN CÓ ĐIỀU KIỆN 2026-08-19 — chủ dự án chọn "chấp nhận nhưng phải đo trước".** Điều kiện: WMS-1 đo p95 lượt đọc phiên trên dữ liệu giả; dưới 10% ngân sách 500 ms của NFR-PER-02 thì ADR chuyển Accepted, trên thì mở lại. T-00 vì thế tách làm WMS-1 (đo) và WMS-2 (dựng lược đồ). Bản viết: `docs/adr/0002-phien-dang-nhap-va-thu-hoi-quyen.md`. Chọn PA-3 (bản ghi phiên trong PostgreSQL, kiểm mỗi request, join cờ hoạt động của tài khoản → thu hồi 0 phút thay vì cận 2 phút). Loại PA-1 (JWT 30 phút vi phạm thẳng NFR-SEC-08), PA-2 (refresh token: vẫn hỏi CSDL, thêm cơ chế, làm chậm đổi kho), PA-4 (Redis: thêm hạ tầng cho đội 1 người và ép trước OPN-03). **Vòng challenger độc lập CHƯA chạy** (chỉ thị đứng về AgentTool) — mục Challenge là tự phản biện. **Chặn T-00.** |
