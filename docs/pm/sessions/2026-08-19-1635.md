# Phiên 2026-08-19 16:35 — /team (autonomy: assisted)

Phiên thứ ba trong ngày. T0 xanh; bảng không đổi so với phiên trước vì mọi thứ
đang nằm ở bàn chủ dự án.

## PM — T0 roll call

- Assigned: preflight + đọc bảng + đếm việc UNBLOCKED.
- Exchange: `preflight.sh` GREEN · `schedule_check` XANH (sprint-1 0.0/8.0 pd,
  7 câu đang cháy) · Jira **0 ticket**. Thứ tự ưu tiên /pm P1: QA debt 0 →
  DEV ticket 0 (chờ gate B4 của chủ dự án) → BA batch #2 **cố ý bỏ qua** (batch
  #1 chưa duyệt; soạn thêm chỉ là chất tồn kho — Little's Law) → **SA: ADR-0002
  tới lượt**, nguồn việc hợp lệ vì nó nằm ở §3 hàng đợi.
- Result: done — một item, chạy tuần tự trên cây làm việc chính (lane nền cần
  worktree + spawn agent, cả hai đều không dùng trong phiên này).

## PM — T1 P-DECIDE

- Assigned: 7 câu đang cháy, trong đó G-01/G-03/G-04 hạn 21/08.
- Exchange: **không hỏi lại**. Cả 7 câu đã trình ở desk report hai phiên trước
  và chủ dự án chọn chạy tiếp; hỏi lần thứ ba trong cùng một ngày là nhiễu chứ
  không phải kỷ luật. Ghi nhận: các câu đang già đi, đưa lên đầu desk report.
- Result: done — SKIP có lý do, không câu nào bị bỏ quên.

## SA — ADR-0002 phiên đăng nhập và thu hồi quyền

- Assigned: ràng buộc chéo BA phát hiện ở batch SC-01 — FR-01-07 (phiên 30 phút
  không thao tác) vs NFR-SEC-08 (vô hiệu hoá tài khoản phải có hiệu lực trong
  tối đa 2 phút). Đọc: `docs/team/roles/sa.md`, ADR-0001, shard SC-01,
  `phi-chuc-nang.md §7.1 và §7.3`, `mo-hinh-du-lieu.md §5.1`, SRS §2.3.
- Exchange (tự phản biện — **KHÔNG spawn challenger agent**, chỉ thị đứng của
  chủ dự án; ghi cảnh báo ngay trong mục Challenge của ADR):
  - 4 phương án có thật, mỗi cái bị loại có lý do đo được: PA-1 JWT 30 phút vi
    phạm thẳng NFR-SEC-08 (gấp 15 lần cận); PA-2 refresh token vẫn hỏi CSDL chỉ
    thưa hơn mà thêm hẳn một cơ chế; PA-4 Redis thêm hạ tầng cho đội một người
    và ép trước OPN-03. Chọn **PA-3**: bản ghi phiên trong PostgreSQL đã có sẵn.
  - Điểm được nhất của PA-3: join cờ hoạt động của tài khoản ngay trong lượt đọc
    phiên → thu hồi **0 phút** thay vì cận 2 phút, và không cần tiến trình quét.
  - Ba hệ quả tiêu cực được viết dài hơn phần tích cực, đúng luật SA #4: thêm
    một lượt đi CSDL mỗi request (**chưa đo — đã biến thành điều kiện bắt buộc
    của T-00 và điều kiện mở lại ADR**); gộp ghi 60 giây làm ngưỡng 30 phút của
    FR-01-07 sai lệch ±60 giây; thêm thực thể `Phien` mà SRS §5.1 không liệt kê.
  - Delta thực thể đã ghi vào `docs/specs/changes.md` để shard và thực tế không
    nói hai chuyện khác nhau.
  - Phát sinh G-05: ADR buộc phải nêu tham số kiến trúc mới có nghĩa, nên
    `docs/adr/` có nên bị gitignore như `docs/specs/` không — và câu này phải
    trả lời **trước lần push đầu tiên**, vì repo chưa push lần nào.
  → chi tiết: docs/adr/0002-phien-dang-nhap-va-thu-hoi-quyen.md
- Result: done — trạng thái dừng ở **Proposed**. Chấp nhận ADR là quyết định
  kiến trúc, thuộc chủ dự án, không phải máy.

## PM — gate B4 và chấp nhận ADR (chủ dự án trả lời trong phiên)

- Assigned: hai câu hỏi cấu trúc cuối phiên — duyệt batch #1, và chấp nhận ADR-0002.
- Exchange: chủ dự án chọn **"Duyệt — tạo ticket ngay"** và **"Chấp nhận nhưng
  phải đo trước"**. Hệ quả: T-00 tách làm hai, WMS-1 đi lấy số p95 trước, WMS-2
  mới dựng lược đồ; ADR-0002 chuyển 🟡 chấp nhận-có-điều-kiện chứ không phải
  Accepted thẳng.
  - **Provider Jira của vteam KHÔNG có phương thức tạo issue** (chỉ đọc, comment,
    transition, link, attach, worklog) → phải gọi thẳng `POST /rest/api/2/issue`.
    Tạo MỘT ticket trước (WMS-1), chạy `dor_check` xác nhận xanh, rồi mới tạo 5
    cái còn lại — không tạo hàng loạt rồi mới kiểm.
  - Nối 7 quan hệ chặn, chạy gate DoR trên cả 6: WMS-1 ✅ sẵn sàng code, 5 cái
    còn lại đỏ đúng thực tế. Ba ticket UI đỏ vì thiếu oracle thiết kế — mô tả
    của chúng ghi thẳng chữ "UI" để gate làm đúng việc, thay vì lách KI-002.
  - `plan.yaml` nạp 6 item: 5.5/8.0 pd, schedule_check vẫn ON SCHEDULE.
  → chi tiết: docs/pm/decisions.md §2.1
- Result: done — **6 ticket có thật trong Jira**, WMS-1 sẵn sàng cho /dev.

## Tổng kết 3 dòng

1. Phiên này biến toàn bộ công của hai phiên trước thành thứ chạy được: **6
   ticket có thật trong Jira, sprint-1 nạp 5.5/8.0 pd, WMS-1 sẵn sàng code** —
   lần đầu tiên trong ngày có một việc máy được phép bắt tay vào.
2. Nút thắt đã đổi chỗ: không còn là hàng đợi quyết định, mà là **gate 2 người
   review của lane /dev** — không spawn được agent thì không ticket nào đi hết
   vòng, kể cả WMS-1.
3. Vẫn còn: 5 commit chưa push (mọi câu hỏi công khai còn rẻ tới lúc đó), G-04
   đang giữ đỏ ba ticket UI, OPN-03 sẽ chặn ngay sau WMS-2.
