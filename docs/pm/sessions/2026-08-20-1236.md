# Phiên 2026-08-20 (chiều) — /team (autonomy: full)

Phiên WMS-3: màn hình đầu tiên của sản phẩm. Code xong, bằng chứng đầy đủ, cổng
xanh 13/13 — và **không merge được**, vì vòng review tìm ra bảy lỗi thật mà 73
test xanh của tôi không chạm tới.

## PM — T0 roll call

- Exchange: preflight GREEN. WMS-2 đã merge ở phiên trước ⇒ WMS-3 hết xích,
  DoR xanh, là ticket kế tiếp của sprint-1.
- Result: done.

## PM — T1 P-DECIDE

- Assigned: hàng đợi không có câu nào tới hạn hôm nay chưa hỏi (Q6/Q7 chưa tồn
  tại lúc T1 — chúng sinh ra TRONG lúc code, xem dưới).
- Result: done — không có câu nào để hỏi ở T1.

## DEV — WMS-3 · S-01 Đăng nhập và khoá tạm sau 5 lần sai

- Assigned: 9 AC ở `docs/backlog/sc-01-dang-nhap-draft.md` §S-01; oracle thị giác
  là bản mẫu SC-01 (G-04); phụ thuộc G-01 (đã có quyết định tạm).
- Exchange:
  - Sáu mô-đun thuần + trang `/login` + server action. Trước khi viết dòng nào,
    xác nhận **không tồn tại quyết định nào về hạ tầng email** ⇒ dựng cổng
    `CongThongBao` thay vì tự chọn nhà cung cấp, và nói thẳng AC4 chưa đạt.
  - Fixture test tích hợp đỏ ở `deleteMany` — hoá ra là ràng buộc WMS-2 làm đúng
    việc: tài khoản đã có vết kiểm toán thì **vĩnh viễn không xoá vật lý được**.
    Sửa fixture theo ràng buộc, không nới ràng buộc cho fixture dễ sống.
  - **Lỗi lược đồ chỉ lộ ra khi bấm nút thật**, sau khi 70 test đã xanh:
    `dia_chi_ip` kiểu `inet` NOT NULL ⇒ ở cấu hình lúc đó MỌI lượt đăng nhập
    thành công trả 500. Sửa bằng migration cho phép NULL, thêm 3 test tích hợp.
  - **ĐÍNH CHÍNH (viết lại cuối phiên).** Dòng trên, bản đầu, còn kèm câu
    *"Next.js server action không đọc được địa chỉ socket"*. **Câu đó SAI** —
    `next/dist/server/base-server.js` có
    `req.headers['x-forwarded-for'] ??= originalRequest?.socket?.remoteAddress`.
    Reviewer R2 bác bằng mã nguồn và bằng phép chạy. Tôi đã chép câu chưa kiểm
    ấy vào năm chỗ, trong đó có một file migration và một câu hỏi gửi chủ dự án.
    Giữ dòng đính chính này thay vì xoá dấu vết: biên bản phiên sống lâu hơn ticket.
  - Bước `e2e` của gate trước nay ĐỎ vì `test:e2e` không tồn tại. Đã viết bộ
    e2e thật (4 test, tự dựng máy chủ trên cổng động, Chromium) ⇒ gate xanh
    trọn 13 bước, 0 bước bỏ qua.
  - Đo độ trùng khớp bằng `getComputedStyle` trên từng cặp phần tử của bản mẫu
    và của app — không dùng so-sánh-pixel, vì hai bên cố ý khác nội dung.
  - Một bằng chứng AC9 bản đầu **sai** (dấu `$` không escape qua shell làm regex
    thành "hết chuỗi", đếm 130/130 "không băm"). Phát hiện vì con số mâu thuẫn
    với mẫu in ra ngay bên dưới. Viết lại truy vấn trong file, không nhồi qua shell.
  - → chi tiết: `evd/WMS-3/REPORT.md` · `evd/WMS-3/dev/fidelity.md`
- Result: **failed** — ba thẻ REQUEST-CHANGES ở vòng 1, ba thẻ nữa ở vòng 2.
  Hết vòng phản biện theo luật /dev ⇒ **không merge**. Bốn khiếm khuyết còn mở,
  liệt kê ở `evd/WMS-3/REPORT.md` §Vòng review.

## PM — hai câu hỏi mới sinh ra trong lúc code

- Assigned: không do đọc spec mà ra; lộ ra ở lần bấm nút thật đầu tiên.
- Exchange: **Q6** nguồn địa chỉ IP khi triển khai (mặc định hiện tại siết cả hệ
  thống về 20 lượt đăng nhập/phút — có thể chặn nhầm người thật giờ đổi ca);
  **Q7** hạ tầng email (chặn AC4, và làm yếu quyết định tạm G-01 vốn *dựa vào*
  email để nói rõ với chủ tài khoản thật). Q7 thuộc `autonomy.exemptions`.
- Result: done — đã vào `docs/pm/decisions.md §1.3`, hạn 2026-08-27.

## Tổng kết phiên

Viết xong màn hình đầu tiên của sản phẩm và **không giao được**: vòng review ba
người tìm ra bảy lỗi thật mà 73 test xanh cùng cổng gate 13/13 của tôi không chạm
tới — cuộc đua khi gửi song song, kênh phụ thời gian, và một tiền đề sai về địa
chỉ IP mà tôi đã chép vào năm chỗ trước khi ai kiểm nó.

Sửa được ba lỗi nặng nhất trong vòng phản biện duy nhất, nhưng vòng 2 lại ra ba
thẻ REQUEST-CHANGES nữa — trong đó hai lỗi là do **chính vòng sửa của tôi đẻ ra**,
và một lỗi là tôi tưởng đã sửa mà thực ra chỉ dọn dẹp hình thức. Hết vòng theo
luật /dev ⇒ ghi `failed`, không merge, `git push` bị hook chặn đúng như thiết kế.

Thứ mang đi được: bốn khiếm khuyết còn mở đều có phép đo đứng sau chứ không phải
linh cảm, mọi câu khai sai đã bị gỡ khỏi hồ sơ, và KI-004 ghi lại bốn phép thử mà
mọi ticket chạm trạng thái dùng chung phải có — gửi song song, bấm giờ ở nhiều
mức đồng thời, dữ liệu rác ở biên, và chạy ở cấu hình SAI.
