# Phiên 2026-08-19 16:26 — /team (autonomy: assisted)

Phiên thứ hai trong ngày. T0 xanh ngay từ đầu nên chạy thẳng vào việc.

## PM — T0 roll call

- Assigned: preflight + đọc bảng + đếm việc UNBLOCKED.
- Exchange: `preflight.sh` GREEN cả 7 chân · `schedule_check` XANH (sprint-1
  0.0/8.0 pd) · Jira `project = WMS` **0 ticket** · `shard_spec --check` 39/39
  khớp nguồn. Theo thứ tự ưu tiên /pm P1: QA debt 0 → DEV ticket 0 → **BA batch
  đầu tiên** → SA ADR 0 (mục §3 hàng đợi rỗng, không tự sinh việc cho SA).
- Result: done — một item duy nhất, chạy tuần tự, không mở lane nền (luật "≥2
  item mới đáng mở").

## PM — T1 P-DECIDE

- Assigned: 4 câu đang cháy (OPN-01/02/03, A3) đều đã trình ở desk report phiên
  trước và chủ dự án chọn chạy tiếp thay vì trả lời.
- Exchange: không hỏi lại trong phiên này (luật "đừng hỏi lại trong cùng phiên").
  Câu chọn slice đầu tiên cũng đã hỏi cuối phiên trước và không được chọn → PM
  tự quyết theo logic phụ thuộc: **SC-01 là gốc của 19 màn hình còn lại** (không
  có danh tính + bối cảnh kho thì không màn hình nào ghi được `NguoiDung`/`Kho`
  vào nhật ký), và nó KHÔNG bị OPN-03 chặn vì phân vùng chỉ chạm `ChuyenDongKho`.
- Result: done — chọn SC-01 làm batch đầu tiên, lý do ghi lại ở trên.

## BA — batch #1: SC-01 (bản nháp chờ gate B4)

- Assigned: shard SC-01 + `phan-quyen.md §8` + `phi-chuc-nang.md §7.3` +
  `srd-yeu-cau-nghiep-vu.md` BR-22 + `mo-hinh-du-lieu.md §5.1` + AS-IS PB-01…08.
- Exchange (tự phản biện — **KHÔNG spawn challenger agent**, chủ dự án có chỉ
  thị đứng "không gọi AgentTool trừ khi được yêu cầu"; đây là chất lượng thấp
  hơn một người đọc độc lập và đã nói rõ ở desk report):
  - Soạn 1 task + 7 story, nạp sprint-1 5.0/8.0 pd, 3 story hoãn sprint-2.
  - Vòng phản biện bắt 4 lỗi của chính bản nháp: (1) AC gộp "hoặc"/"và" vi phạm
    tính đơn nhất ISO 29148 → tách thành 5 AC; (2) T-00 không có Given/When/Then
    nên sẽ trượt `dor_check.py:44` → thêm 4 AC; (3) thiếu chuỗi `out of scope`
    tiếng Anh mà regex `OOS` đòi; (4) chưa trỏ oracle thị giác.
  - **Phát hiện lớn nhất không nằm trong spec:** `docs/StockFlow-WMS-20-screens.html`
    là prototype CHẠY ĐƯỢC của cả 20 màn hình (React bundle, route `/login`,
    style nội tuyến, nav `data-sc`), trong khi `design.provider: none` → /dev
    đang không có oracle thị giác nào. → G-04.
  - 4 khoảng trống qua bar 3 điều kiện → hàng đợi §1.2; 1 ràng buộc chéo
    (FR-01-07 phiên 30 phút vs NFR-SEC-08 thu hồi trong 2 phút) là quyết định
    kiến trúc → ADR-0002 cho SA, không để BA chọn hộ.
  → chi tiết: docs/backlog/sc-01-dang-nhap-draft.md (local, gitignore) ·
    docs/pm/decisions.md §1.2 và §3
- Result: done — **dừng đúng ở gate B4**: `autonomy: assisted` nên chủ dự án
  duyệt bản nháp trước, chưa tạo ticket Jira nào.

## Tổng kết 3 dòng

1. Batch đầu tiên đã có hình: 1 task + 7 story, 5.0/8.0 pd nạp sprint-1 — nhưng
   **vẫn 0 ticket trong Jira** vì gate B4 thuộc về chủ dự án, không phải máy.
2. Ba câu chặn sprint-1 (G-01 mâu thuẫn nội tại của SC-01, G-03 người dùng chưa
   có kho, G-04 oracle thị giác) hạn 21/08 — trả lời xong là /dev chạy được.
3. Việc quan trọng nhất phiên này không phải viết story mà là hai phát hiện:
   có sẵn prototype 20 màn hình chưa ai nối vào chuỗi, và `dor_check` dò bằng
   chuỗi tiếng Anh trong một dự án viết tiếng Việt (KI-002).
