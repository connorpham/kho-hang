# Phiên 2026-08-21 — /team (autonomy: full)

Ngày mở khoá: chủ dự án gỡ A11, WMS-3 được thử lần thứ ba với phạm vi đã chốt.

## PM — T0 roll call

- Exchange: preflight GREEN. Bảng việc: WMS-1/WMS-2 Done · **WMS-3 In Progress,
  bị A11 khoá** · WMS-4/5/6 To Do nhưng cả ba nằm sau phiên đăng nhập của WMS-3
  ⇒ **0 ticket DEV dispatch được**. PR #6 (sổ sách 20/08) mở, CI xanh, chờ chủ
  dự án. ADR-0003 Proposed.
- Result: done — T1 là toàn bộ giá trị của buổi sáng.

## PM — T1 P-DECIDE: A11 · OPN-03 · OPN-01

- Assigned: ba dòng đến hạn trong sprint (A11 hạn hôm nay; OPN-01/03 hạn 26/08).
- Exchange: **A11 = (a)** gỡ khoá một vòng, làm phương án chặn hàng đợi —
  phương án cả ba reviewer độc lập cùng chỉ vào. **OPN-03 = đám mây công cộng**
  ⇒ ADR-0001 hết điều kiện treo, phân vùng `ChuyenDongKho` theo tháng chốt được,
  A8 vế (b) mở khoá, và sinh một câu mới chưa đăng ký hạn (nơi đặt dữ liệu theo
  Nghị định 53). **OPN-01 = chỉ 1 sàn cho Giai đoạn 1**, sàn nào thì chưa chốt.
- Result: done — WMS-3 dispatch lại được.

## PM — hai việc tự làm được, không cần hỏi

- ADR-0003 → **Accepted**: theo ngoại lệ luật /team, nó chỉ mô tả thứ WMS-2 đã
  merge. Đếm lại `Kho` = 6 cột scalar trước khi chuyển (ADR này từng sai chính
  phép đếm đó). ADR-0001 → **Accepted không điều kiện** theo OPN-03.
- Sửa một lỗi sổ: dòng OPN-02 mất mã khoá ở ô đầu nên không tra được.
  **Đính chính:** tôi ban đầu báo "OPN-02 chưa được ghi" — sai, tôi đọc bản
  `develop` cũ hơn một ngày; câu trả lời 20/08 đã có đủ kèm dòng hệ quả.

## DEV — WMS-3 lần thử 3 (`47a9764` → `1da119a`)

- Assigned: phạm vi chủ dự án chốt ở A11(a) — van chặn hàng đợi, từ chối khởi
  động khi sàn quá thấp, dụng cụ đo đọc bậc từ CSDL + khách HTTP thô + tự vô
  hiệu, cộng năm khiếm khuyết V2–V6.
- Exchange: **sửa dụng cụ đo TRƯỚC, mã sản phẩm sau** — ba vòng trước tôi làm
  ngược lại. Vòng review 3 người: R1 APPROVE-with-questions, R2 + R3
  REQUEST-CHANGES. Phát hiện nặng nhất: **van của chính tôi phá AC3** (gạt lượt
  sai thứ năm ⇒ tài khoản không bao giờ khoá). Vòng phản biện: đổi GẠT sang
  XẾP HÀNG. Tái review: **cả ba REQUEST-CHANGES** — cửa gạt dời ra trần hàng
  đợi và chữ ký cũ sống lại ở đó.
  → chi tiết: [review.md §Vòng 4](../../../evd/WMS-3/dev/review.md)
- Result: **failed** — hết vòng phản biện. Dòng hỏng thứ BA ⇒ A12.

## PM — cải chính hồ sơ và đóng ngày

- Assigned: sửa mọi câu sai reviewer chỉ ra (bắt buộc, không phụ thuộc verdict).
- Exchange: chín chỗ. Nặng nhất không phải con số lẻ mà là **tệp bằng chứng
  kênh phụ tôi commit là của bản TRƯỚC khi sửa và chưa hề chạy lại** — REPORT
  trích một tệp không mô tả mã đang commit. Đã sinh lại. Hai câu tôi viết HÔM
  NAY trong `.env.example` sai theo hai chiều ngược nhau. AC7 hạ từ ✅ vô điều
  kiện xuống ĐẠT CÓ ĐIỀU KIỆN.
- Result: done — A12 vào hàng đợi với ba lối; KI-006 vào knowledge-base; cổng
  `evd_tuoi_check.py` viết xong (chưa nối `gates.yaml` vì đó là đường
  high-stakes), đã chứng minh bắt được đúng lỗi hôm nay.

## Tóm tắt phiên

1. **WMS-3 vẫn không merge được, lần thứ ba** — nhưng lần này kênh phụ đã đi từ
   "mở toang 3,25×" xuống "đóng ở mọi tải van cho chạy, phần dư 1–2 ms", và R1
   xác nhận ĐỘC LẬP bằng khách HTTP tự viết ở K từ ½× tới 10× trần.
2. **Bản vá bảo mật của tôi phá một tính năng, rồi bản vá của bản vá dời lỗi ra
   một cửa khác.** Hai lần trong một ngày, cùng một khuôn → KI-006.
3. **Còn cần chủ dự án:** A12 (AC7 đạt ở phạm vi nào — ba lối) · PR #6 chờ merge
   · Q7 phần 2 · A8 · OPN-01 (sàn nào) · OPN-02 (cấp duyệt).
