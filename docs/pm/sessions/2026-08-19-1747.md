# Phiên 2026-08-19 (chiều muộn) — /team (autonomy: assisted)

Phiên thứ tư. Một item duy nhất: WMS-1, lần thử 2/2.

## PM — T0 roll call

- Assigned: preflight + đọc bảng + đếm việc UNBLOCKED + đếm sổ trước khi điều phối.
- Exchange: preflight GREEN · sprint-1 5.5/8.0 pd, ON SCHEDULE · Jira 6 ticket,
  chỉ WMS-1 qua DoR · sổ có **1 dòng hỏng** cho WMS-1 → còn đúng một lần thử tự
  chọn. QA debt 0, BA batch #2 không soạn (batch #1 đã thành ticket, soạn thêm là
  chất tồn kho), mục ADR trong hàng đợi rỗng sau khi ADR-0002 xong.
- Result: done — một item, chạy tuần tự.

## PM — T1 P-DECIDE

- Assigned: 8 câu đang cháy.
- Exchange: **không hỏi lại lần thứ tư**. Cả 8 đã trình ở desk report ba phiên
  liền; hỏi lại trong cùng một ngày là nhiễu. Ghi nhận chúng đang già đi và đưa
  lên đầu desk report.
- Result: done — SKIP có lý do.

## DEV — WMS-1 lần thử 2/2 (FAILED, đã leo lên bàn chủ dự án)

- Assigned: 11 finding chặn của vòng 1 (`evd/WMS-1/dev/review.md`).
- Exchange:
  - **Viết lại toàn bộ** `scripts/do-p95-phien.mjs`: tách thời gian truy vấn khỏi
    thời gian chờ pool · 5 lần lặp × 2 hồ sơ dữ liệu, phán quyết lấy lần xấu nhất ·
    làm nóng pool · mã phiên 64 ký tự hex phân tán · join 3 bảng theo NFR-SEC-04 ·
    bỏ phán quyết NFR-PER-05 (tiêu chí mức màn hình, chưa có màn hình) · bỏ
    `Math.max` hai tổng thể · rào chắn host + SIGINT/SIGTERM + `lock_timeout` +
    từ chối ghi đè schema. Thêm `scripts/sinh-bao-cao-wms1.py` **sinh REPORT.md từ
    JSON** để không còn con số chép tay nào.
  - Hai lỗi do chính guard mới bắt trong lúc viết: lần chạy đầu tự dừng ở
    `284/300 lượt trúng phiên` (5% phiên thuộc tài khoản đã khoá); lần chạy hỏng
    đó **để lại schema** vì `finally` ngoài cùng chỉ đóng kết nối — đúng lỗi R2
    cảnh báo, khác đường đi. Đã chuyển dọn dẹp vào `finally`, chỉ dọn khi chính
    script đã tạo schema.
  - **Vòng 2: R2 APPROVE** (cả 4 finding chặn đã xử lý, tái hiện từng cái).
    **R1 REQUEST-CHANGES** — 6 đã xử lý, 3 nửa vời, 4 finding mới. R1 đánh kết
    luận 8 lần, **không lật lần nào**, và tái sinh REPORT.md từ JSON ra
    byte-identical (lỗi liêm chính bằng chứng của vòng 1 đã đóng hẳn).
  - **MỚI-1 là thứ chặn thật, và nó không phải lỗi code:** ADR-0002 viết điều kiện
    là "mỗi request cộng thêm một lượt đi CSDL", mà lấy kết nối là một phần của
    lượt đi đó. Tôi tách chờ pool ra khỏi phán quyết để sửa finding vòng 1 — và
    chính việc tách đó giữ phán quyết ở ĐẠT. R1 đo riêng chờ pool p95 vượt trần
    50 ms ở **2/6 lần chạy**. Vòng 1 bảo "anh đang đo cái harness"; vòng 2 bảo
    "tách đúng nhưng anh vừa loại một chi phí có thật". Cả hai đều đúng.
  - Hết vòng phản biện theo luật /dev, không có vòng ba. Không sửa CODE ĐO sau
    review (cách đọc quyết định thứ phải đo); chỉ gắn khối ⚠️ lên REPORT.md để
    một phán quyết đang tranh cãi không đứng như sự thật đã chốt.
  → chi tiết: evd/WMS-1/dev/review.md · docs/pm/decisions.md A4
- Result: failed: vòng review 2 người — **dòng hỏng thứ hai**. Theo loop guard #3
  đã đăng ký **A4** ở mục việc-của-chủ-dự-án kèm cả hai lý do hỏng; máy không tự
  chọn lại WMS-1 cho tới khi A4 được gỡ.

## Tổng kết 3 dòng

1. Sprint-1 vẫn tiêu 0 pd. Ticket duy nhất chạy được đã hỏng hai lần ở vòng
   review và giờ nằm trên bàn chủ dự án — đó là kết quả trung thực của ngày.
2. Điều đáng giá không phải con số 2,271 ms mà là câu hỏi nó lôi ra: **"một lượt
   đi CSDL" có gồm chi phí lấy kết nối không?** Trả lời (a) thì ADR-0002 phải mở
   lại và kích thước pool thành quyết định kiến trúc; trả lời (b) thì ADR phải nói
   thẳng điều đó. Không cách nào để máy tự chọn.
3. Vòng review đã đánh hỏng sản phẩm đầu tiên hai lần liên tiếp, mỗi lần vì một
   lý do khác, và cả hai lần đều kèm lệnh tái hiện. Đây là gate đắt nhất trong
   quy trình và cũng là gate đang tạo ra nhiều giá trị nhất.
