# Phiên 2026-08-20 — /team (autonomy: full · self_merge: true)

Ngày thứ hai của sprint-1. Phiên đầu tiên có thứ đi hết vòng: một PR merge và một
ticket đóng.

## PM — T0 roll call

- Assigned: preflight + đếm việc UNBLOCKED.
- Exchange: preflight GREEN · sprint-1 còn 7,2 pd · WMS-1 xanh DoR nhưng bị **A6**
  khoá, WMS-2…6 xích sau nó ⇒ **0 ticket dispatch được**. Nhưng có một việc đã
  QUYẾT mà chưa thi hành: **G-05 cho phép push, 15 commit vẫn nằm ở máy**.
- Result: done — chọn thi hành G-05 làm việc của phiên; đó là chấp hành một quyết
  định đã ghi, không phải máy tự sinh việc.

## DEVOPS — lần push đầu tiên, PR #2, CI lần đầu chạy

- Assigned: đưa 8 commit sổ sách lên origin theo G-05.
- Exchange: **quét trước khi làm điều không rút lại được.** Hai cảnh báo, cả hai
  được làm rõ: `docs/specs/*` xuất hiện trong diff là do **xoá** (17 dòng deletion
  — chính lệnh untrack của Q4), không phải thêm; các dòng yêu cầu nằm trong ADR-0002
  đúng thứ G-05 đã cho phép. Nhưng quét lộ một chỗ **ADR tự mô tả sai chính nó**
  ("chỉ tham chiếu bằng mã, không chép nguyên văn" — vế sau đúng, vế đầu sai vì
  bảng §Bối cảnh có tóm tắt nội dung yêu cầu). Sửa trước khi push.
  Push nhánh chore → PR #2 → **CI PASS trên bản clone sạch trong 50 s**. Đây là
  xác nhận thực nghiệm cho phân tích Q4: gate còn xanh khi `docs/specs/` không tồn
  tại trong clone.
  PR xanh, không chạm exemption, không chạm `src/`/`prisma/` ⇒ đủ điều kiện
  **tự merge** ở mức full. Đã merge; `develop` cục bộ đồng bộ lại với origin.
- Result: done — origin/develop có sổ sách 3 phiên đầu; `e6c050f`.

## PM — T1 P-DECIDE: A6

- Exchange: chủ dự án chọn **(a) nhận con số, bỏ harness**.
- Result: done.

## DEV — WMS-1 ĐÓNG theo A6

- Assigned: thi hành A6.
- Exchange: xoá `scripts/do-p95-phien.mjs` và `scripts/sinh-bao-cao-wms1.py` — qua
  3 lần viết và 5 vòng review vẫn còn 5 khiếm khuyết, hai trong đó nguy hiểm nếu
  ai chạy lại. **Bằng chứng giữ nguyên**: `do-p95.json` không sửa một byte, để
  chuỗi kiểm chứng độc lập của R1 còn giá trị. Viết lại `REPORT.md` thành bản ghi
  cuối (trình sinh đã xoá nên nó không sinh lại được), trong đó **cải chính hai
  khẳng định sai** từng nằm trong hồ sơ: `r = 0,920` là hằng số viết cứng (dữ liệu
  thật cho 0,706), và trường `khongChamDia` nói EXPLAIN đã chạy trong khi script
  chưa từng chạy — người đo là R1 ở vòng review.
  ADR-0002 → **Accepted**. Comment 7 phần vào WMS-1 rồi chuyển **Done**.
  → chi tiết: evd/WMS-1/REPORT.md · evd/WMS-1/dev/review.md
- Result: done — **WMS-2 hết bị chặn, DoR xanh.**

## SA — ADR-0003 mô hình dữ liệu danh tính

- Assigned: WMS-2 yêu cầu mô hình hoá 4 thực thể, mà `prisma/schema.prisma` đặt
  luật "34 thực thể chưa có đặc tả cột thì KHÔNG được mô hình hoá ở đây".
- Exchange: kiểm tra xác nhận **SRS §5.2 chỉ đặc tả cột cho 3 thực thể**
  (SoDuTonKho, ChuyenDongKho, SerialItem) — không thực thể nào của WMS-2. Nên ADR
  trước, lược đồ sau, với một luật duy nhất: **mỗi cột truy được về một yêu cầu;
  chỗ nào không truy được thì không có cột**. `Kho` cố ý tối thiểu vì cấu trúc bốn
  cấp thuộc SC-06 và chờ TBD-01.
- Result: done — Proposed, chấp nhận là quyền chủ dự án.

## DEV — WMS-2 (FAILED lần 1/2 ở vòng review 3 người)

- Assigned: ticket high-stakes đầu tiên (chạm `prisma/schema.prisma` và
  `prisma/migrations/`) ⇒ 3 reviewer, cả ba `opus`.
- Exchange:
  - Vòng 1: **12 finding chặn**. R1 bắt AC3 không đạt và tác giả trích AC thiếu bảy
    chữ; R2 chứng minh migration không nguyên tử và trigger hụt ở TRUNCATE; R3 chạy
    8 đột biến, **6 phép để bộ test xanh nguyên**.
  - **R2 giải được câu đố tôi ghi là "chưa quy được nguyên nhân": hai PostgreSQL
    cùng đòi cổng 5432.** Bind loopback của Homebrew thắng bind wildcard của
    container ⇒ toàn bộ migration và test vòng 1 chạy nhầm server. → KI-003.
  - Vòng sửa: chạy lại trên container; timestamptz; 3 khoá ngoại; `nguoi_dung_id`
    nullable; migration dựng lại một file có `BEGIN/COMMIT`, trigger TRUNCATE,
    `ENABLE ALWAYS`, `NULLS NOT DISTINCT`, seed idempotent; test 13 → 27 kiểm hành
    vi; chủ dự án quyết **A7** (chỉ seed QTHT toàn quyền) để phá deadlock khởi tạo.
  - Vòng 2: **R1 APPROVE · R2 APPROVE · R3 REQUEST-CHANGES**. 7/8 đột biến cũ chết,
    nhưng 7 phép mới lọt — nặng nhất là mật khẩu **rõ** trong `mat_khau_hash` vẫn
    27/27 xanh, và `ENABLE ALWAYS` không có test canh (R2 và R3 độc lập cùng tìm ra).
  → chi tiết: evd/WMS-2/dev/review.md
- Result: failed: vòng review 3 người (2/3 chữ ký) — còn 5 việc rẻ và cụ thể, và
  còn một lần thử tự chọn trước loop guard #3.

## Tổng kết 3 dòng

1. Sprint-1 tiêu **0,5 pd** (WMS-1 đóng). Lần đầu repo có PR merge, CI chạy thật
   trên clone sạch, và một migration có thật — nhưng WMS-2 chưa qua cổng review.
2. Phát hiện đắt nhất trong ngày không nằm trong diff: **hai PostgreSQL cùng cổng
   5432** khiến cả migration lẫn test chạy nhầm server suốt một vòng, và
   `db-check.sh` xanh suốt vì nó chỉ chạy `SELECT 1`.
3. Lỗi của riêng tôi, lần thứ tư cùng một hình dạng: khẳng định về công việc của
   chính mình mà không kiểm lại — trích AC thiếu bảy chữ, hai câu "đã ghi tài liệu"
   khi file không tồn tại, ADR đếm sai số cột của chính nó. R3 gọi đúng tên bản
   sửa của tôi: "vá theo phép đột biến, không phải vá theo lớp lỗi".
