# Phiên 2026-08-19 19:09 — /team (autonomy: assisted → **full** trong phiên)

Phiên thứ năm. Mở đầu bằng 0 việc dispatch được, kết thúc bằng 4 quyết định của
chủ dự án và một ticket hỏng lần thứ ba — nhưng hỏng ở chỗ khác hẳn hai lần trước.

## PM — T0 roll call

- Assigned: preflight + đếm việc UNBLOCKED.
- Exchange: preflight GREEN · Jira 6 ticket, chỉ WMS-1 xanh DoR nhưng **bị loop
  guard #3 khoá** · WMS-2…6 đỏ vì phụ thuộc · BA batch #2 không soạn (tồn kho) ·
  §3 hàng đợi chỉ có ADR-0002 cũng chờ A4. **Việc dispatch được: 0.**
- Result: done — luật bắt dừng sớm, nên việc giá trị nhất là T1.

## PM — T1 P-DECIDE (hỏi lại, lần này có lý do)

- Assigned: 8 câu cháy. Ba phiên trước tôi bỏ qua vì đã trình rồi; phiên này hàng
  đợi không còn là "nên dọn sớm" mà là **thứ duy nhất tạo ra được tiến độ**.
- Exchange: hỏi 4 câu đòn bẩy cao nhất. Chủ dự án trả lời cả 4:
  - **A4 = "CÓ, lượt đi CSDL gồm chi phí lấy kết nối"** → WMS-1 TRƯỢT theo cách
    đọc đúng; kích thước pool thành quyết định kiến trúc; loop guard được gỡ.
  - **G-04 = công nhận prototype 20 màn hình là oracle thị giác** → gắn `mockup:`
    vào WMS-3/4/5, cả ba hết đỏ ở luật design-link.
  - **G-05 = giữ ADR trong repo, được push.**
  - **Nâng `autonomy.level` lên `full` + bật `self_merge`** (ghi thành A5).
- Result: done — áp dụng full có chọn lọc ngay: **G-01 và G-03 quyết tạm 🟡**, cả
  hai chọn phương án đảo ngược rẻ hơn, kèm lý do và đường lùi. **OPN-01/02/03
  KHÔNG tự quyết**: dính `purchasing`/`real-money` và chủ trì là người ngoài.
  → chi tiết: docs/pm/decisions.md §1.2, §2

## SA — ADR-0002 Sửa đổi 1 và 2

- Assigned: hệ quả của A4.
- Exchange: **Sửa đổi 1** — trần 50 ms áp cho toàn bộ chi phí thêm của một request;
  quy tắc chọn pool là ràng buộc TỔNG (`số bản sao × pool + dự phòng ≤
  max_connections`) chứ không phải một con số ma thuật; đặt tạm pool 20 × 3 bản sao.
  **Sửa đổi 2** — sau khi R1 chứng minh điều kiện "lấy lần xấu nhất" hỏng từ gốc:
  viết lại thành p95 GỘP trên ≥5.000 request, báo kèm p99/max/số vượt trần. Ghi
  thêm hai điều số liệu KHÔNG chống lưng cho Sửa đổi 1: phần đuôi là cú khựng cả
  máy (r=0,920) chứ không phải hàng đợi pool, và "pool lớn hơn không tốt hơn" chỉ
  đúng với pool 40, còn pool 10 và 20 không phân biệt được.
- Result: done — vẫn Proposed, chấp nhận là quyền chủ dự án.

## DEV — WMS-1 lần viết 3 (FAILED lần thứ ba, đã leo lên A6)

- Assigned: A4 gỡ khoá; đo lại theo cách đọc mới.
- Exchange: viết lại đo TỔNG theo từng lượt, quét pool 10/20/40, 7 lần lặp.
  **Vòng 1:** R2 tìm ra hồi quy do chính bản sửa trước gây ra (handler tín hiệu
  dùng chung kết nối → 2/3 lần in "đã dọn" trong khi 2 triệu dòng còn nguyên);
  R1 tìm ra CHẶN A — tiêu chí "max qua các lần lặp" đo thời lượng đo chứ không đo
  hệ thống, lật được bằng đổi 7→60 lần lặp.
  **Vòng sửa:** p95 GỘP + cỡ mẫu tối thiểu + mẫu thô vào JSON; handler mở kết nối
  riêng; khoá tư vấn; `allSettled` khi làm nóng pool; suy giảm tính cùng loại.
  **Vòng 2 (duy nhất):** CHẶN A **đóng hẳn** và con số **lần đầu kiểm chứng được
  độc lập** (R1 tính lại từ 14.000 mẫu thô, khớp `numpy` tới chữ số cuối: 5,2031 ms,
  0/14.000 request vượt trần). Nhưng cả hai vẫn REQUEST-CHANGES: trình sinh còn hai
  đoạn kết luận viết cứng nên một lần chạy TRƯỢT 86% vẫn in "đủ điều kiện Accepted";
  và 2/4 lần SIGINT sớm bị lờ hoàn toàn.
  → chi tiết: evd/WMS-1/dev/review.md
- Result: failed: vòng review 2 người — **dòng hỏng thứ ba**. Đăng ký **A6**, nhưng
  A6 hỏi về PHẠM VI chứ không xin thêm vòng sửa: thứ ticket được giao đi lấy thì
  đã có và đã kiểm chứng được; cái còn hỏng là harness.

## Tổng kết 3 dòng

1. Phiên này đổi được nhiều thứ đứng im suốt bốn phiên: 4 quyết định chốt, mức tự
   chủ lên `full`, 3 ticket UI hết đỏ oracle, ADR-0002 có hai bản sửa có số liệu.
   **Nhưng sprint-1 vẫn tiêu 0 pd** — không ticket nào đóng.
2. WMS-1 hỏng lần ba, và lần này khác hẳn: con số **đã đúng và đã kiểm chứng được
   độc lập**; cái hỏng là công cụ và báo cáo quanh nó. Đó là lý do A6 hỏi phạm vi
   chứ không xin sửa tiếp.
3. Bài học đắt nhất là của tôi: tôi tự ghi vào knowledge-base bài học "bằng chứng
   phải sinh từ một lần chạy, không nối tay", rồi ngay ticket sau nhét một hằng số
   `r = 0,920` và một chuỗi EXPLAIN chưa từng chạy vào chính file bằng chứng.
