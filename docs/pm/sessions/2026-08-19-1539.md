# Phiên 2026-08-19 15:39 — /team (autonomy: assisted)

Ngày này gãy làm hai nửa: mở ra thì preflight ĐỎ và ngày dừng ở T0; chủ dự án gỡ
hai chân đỏ ngay trong phiên nên ngày chạy tiếp từ T1.

## PM — T0 roll call (lần 1: DỪNG)

- Assigned: mở ngày làm việc, đọc sổ (plan.yaml · decisions.md · log.md ·
  specs/INDEX.md · tracker), đếm việc UNBLOCKED.
- Exchange (tự chạy, không spawn agent — luật "không spawn cho việc vặt"):
  `preflight.sh` → ❌ Tracker (`/rest/api/3/myself` trả về không phải JSON),
  ❌ Database (cổng 5432 đóng; Docker daemon tắt; `/usr/local/bin/docker` là
  symlink gãy trỏ `/Volumes/Docker/…` đã tháo; binary thật nằm trong
  `/Applications/Docker.app/…`). `schedule_check.py` ĐỎ: hôm nay không thuộc
  sprint nào. `log_check.py` xanh. Việc UNBLOCKED đếm được: **0**.
- Result: blocked: preflight ĐỎ (tracker + DB) — trình bước gỡ, không điều phối
  item nào.

## PM — T0 roll call (lần 2: XANH)

- Assigned: chạy lại sau khi chủ dự án sửa `.env` (A1) và bật Docker (A2).
- Exchange: máy chạy `docker compose up -d db` (gọi bằng đường dẫn tuyệt đối vì
  symlink gãy) → `db-check.sh` = `DB reachable`. `preflight.sh` → **GREEN** cả 7
  chân. Hỏi tracker: `project = WMS` có **0 ticket**.
  → Bảng vẫn trống: 0 sprint item · 0 ticket · 0 shard spec · `specs.sources: []`.
- Result: done — ngày chạy tiếp; theo thứ tự ưu tiên /pm P1 (QA debt → DEV →
  BA → SA) thì việc duy nhất có thật là **lane BA**.

## PM — T1 P-DECIDE (Q1 · Q2 · Q3)

- Assigned: dọn bàn quyết định trước khi làm; 3 câu hỏi hỏi thành 1 lượt.
- Exchange:
  - **Q2 → ✅** "bắt đầu từ hôm nay" → sprint-1 **19/08 → 01/09** (10 ngày làm
    việc × 0.8 = 8 pd) ghi vào `plan.yaml`; `schedule_check` chuyển ĐỎ → **XANH**.
  - **Q1 → 🟡** "đặt mốc tạm" → `project.go_live: 2026-11-24`, số học ghi ngay
    tại config. Hệ số 1.5 pd/màn hình và 15 pd phần nền là PHỎNG ĐOÁN, chưa có
    ước lượng thật chống lưng — thay ngay khi BA có backlog.
  - **Q3 → ✅ (c)** giữ repo public, source chỉ ở local. Máy KIỂM hệ quả thay vì
    tin lời: `verbatim_gate.py:37-38` `sys.exit` khi source thiếu → chạy thử ra
    `SystemExit -> source document missing`; `verbatim` là step trong
    `gates.yaml`, CI chạy `gate.sh` trên mọi push/PR ⇒ **khai báo `specs.sources`
    = CI đỏ vĩnh viễn**. Vậy `specs.sources` phải giữ RỖNG và gate `verbatim`
    không canh gì suốt vòng đời quyết định này. Đã thêm `docs/specs/sources/`
    vào `.gitignore` — thiếu dòng đó thì chính (c) tự phản bội mình.
  - **Q4 phát sinh → 🔴** Q3 mới xử lý *source*, chưa xử lý *shard* — mà shard
    cũng là trích nguyên văn và cũng nằm trong repo public.
  → chi tiết: docs/pm/decisions.md §1
- Result: done · Q2 chốt, Q1 tạm, Q3 chốt kèm 2 hệ quả đã đo, Q4 mở (hạn 20/08).

## BA — nguồn hoá SRS/SRD sang markdown

- Assigned: việc đầu tiên của BA theo chính comment trong `vteam.config.yaml`;
  chạy tuần tự trên main track (không mở lane nền: luật "≥2 item mới đáng mở").
- Exchange: viết `scripts/docx_to_md.py` (commit — vì bản nguồn không commit được
  thì phải TÁI TẠO được) → sinh `docs/specs/sources/SRS.md` (1202 dòng) và
  `SRD.md` (598 dòng), cả hai bị gitignore. Kiểm bằng đường ĐỘC LẬP với script:
  **3524 đoạn/ô có chữ, THIẾU 0**; hai lần chạy ra cùng sha256. Một cảnh báo giả
  ("thiếu 41 mã") đã bị bác bỏ và ghi lại nguyên nhân trong bằng chứng.
  Kiểm kê: **186 FR riêng biệt / 20 màn hình**, 249 hàng mã yêu cầu, và **13
  điểm mở do chính spec khai báo** (7 TBD + 6 OPN) → đăng ký 3 cái đã đến hạn
  theo spec vào hàng đợi, 10 cái còn lại chờ việc tới gần mới đặt hạn.
  → chi tiết: evd/BA-nguon-hoa-spec/REPORT.md · docs/pm/decisions.md §1.1
- Result: done — nhưng **dừng trước bước shard**: shard bị Q4 chặn.

## Tổng kết 3 dòng

1. Ngày mở bằng 0 việc chạy được và đóng bằng: hạ tầng xanh toàn bộ, sprint-1
   mở và ON SCHEDULE, spec đã đọc được bằng máy — nhưng **vẫn 0 dòng code và 0
   ticket**, vì repo chưa có ticket nào và shard đang chờ Q4.
2. Nút thắt duy nhất cho ngày mai là **Q4** (shard có commit lên repo public
   không) — trả lời xong thì BA shard → backlog → ticket → /dev chạy liền mạch.
3. Rủi ro lớn nhất không nằm ở tiến độ mà ở **OPN-03**: hạ tầng chưa chốt trong
   khi migration đầu tiên phải viết SQL phân vùng thủ công — đoán sai thì phải
   viết lại migration đã chạy trên dữ liệu tồn kho thật.
