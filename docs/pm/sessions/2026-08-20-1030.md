# Phiên 2026-08-20 (chiều) — /team (autonomy: full)

Một item duy nhất: WMS-2 lần thử 2/2.

## PM — T0 roll call

- Exchange: preflight GREEN, `db-check.sh` xác nhận đúng container 17.10 (KI-003
  đã xử lý). Sổ: WMS-2 có 1 dòng hỏng ⇒ còn một lần thử tự chọn. Nhánh chưa push
  nên migration viết lại được an toàn.
- Result: done — dispatch WMS-2 lần 2.

## DEV — WMS-2 lần thử 2 (FAILED, leo lên A9)

- Assigned: 5 việc R3 đòi + 6 ghi nhận của R2.
- Exchange:
  - Vòng sửa 1: ràng buộc CSDL cho định dạng băm (**cho phép cả ba họ thuật toán
    NFR-SEC-03 nêu tên**, không chỉ argon2 như R3 gợi ý — chặn hẹp hơn spec là tự
    thu hẹp spec; R3 sau đó **rút lại gợi ý của chính họ**); test
    `session_replication_role`; khẳng định trọn 10 cặp vai trò; ba test rẻ.
  - **Tự chạy lại 7 phép đột biến TRƯỚC khi gửi review** và bắt được lỗi không ai
    nêu: hai test khoá ngoại xanh vì lý do sai (khẳng định `23503` mà không phân
    biệt khoá ngoại nào). Sửa theo lớp: khẳng định TÊN ràng buộc ở 12 chỗ.
  - Vòng review 1: **R2 APPROVE** + 6 ghi nhận (nặng nhất: ràng buộc chống mật
    khẩu rõ **lại in mật khẩu rõ** qua `DETAIL` ⇒ NFR-SEC-09). **R3
    REQUEST-CHANGES**: mọi bản vá nhắm đúng đối tượng R3 gọi tên — 10/12 khoá
    ngoại, 3/6 chỉ mục, cả bảng `phien` gỡ được mà 38/38 xanh.
  - Vòng sửa 2: thay ba danh sách cấm bằng MỘT ảnh chụp lược đồ khẳng định tập.
    11/11 phép đột biến chết, gồm ba phép chưa ai nêu.
  - **Commit đè lên một gate ĐỎ** vì đường ống nuốt mã thoát — lỗi quy trình của
    tôi, đã sửa và ghi vào commit message.
  - Vòng review 2: **CHẶN-1 và CHẶN-1b ĐÓNG** (26/26 phép cũ nay đỏ), nhưng
    **CHẶN-2**: ảnh chụp ghi ĐỊNH DANH, không ghi ĐỊNH NGHĨA. R3 áp 9 phép cùng
    lúc rồi sinh lại ảnh chụp trên CSDL đã hỏng → **không một byte khác nhau**.
  → chi tiết: evd/WMS-2/dev/review.md
- Result: failed: vòng review — dòng hỏng thứ hai ⇒ **A9**.

## Tổng kết 3 dòng

1. Sprint-1 vẫn 0,5 pd. WMS-2 qua ba reviewer, hai duyệt, và **lược đồ sản phẩm
   được xác nhận đúng** — thứ chặn là độ sâu của hàng rào hồi quy, không phải
   khuyết tật trong thứ ticket giao.
2. Tiến bộ đo được giữa hai vòng: từ 6/8 phép đột biến lọt, xuống 0/26. Cái làm
   được điều đó là **một** khẳng định về tập, không phải mười một bản vá.
3. Lỗi của tôi vẫn cùng một hình dạng, nay lần thứ sáu: "38 test" khi thật là 51,
   "ba phép chưa ai nêu" khi thật là một. Và một lỗi mới: tin dòng `tail` thay vì
   tin exit code, nên commit đè lên gate đỏ.
