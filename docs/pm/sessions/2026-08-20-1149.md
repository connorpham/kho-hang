# Phiên 2026-08-20 (trưa) — /team (autonomy: full)

Phiên WMS-2 merge được. Ticket code đầu tiên đi hết vòng.

## PM — T0 roll call

- Exchange: preflight GREEN. Đếm việc: **0 dispatch được** — WMS-2 xanh DoR nhưng
  2 dòng hỏng ⇒ loop guard #3 khoá; WMS-3…6 xích sau nó; BA batch #2 là tồn kho;
  §3 hàng đợi không có ADR chờ viết.
- Result: done — việc giá trị nhất là hỏi A9.

## PM — T1 P-DECIDE: A9

- Exchange: chủ dự án chọn **(a) gỡ khoá cho một vòng nữa**.
- Result: done.

## DEV — WMS-2 lần thử 3 → **MERGE**

- Assigned: CHẶN-2 của R3 — ảnh chụp ghi định danh, không ghi định nghĩa.
- Exchange:
  - Viết lại ảnh chụp dùng `pg_get_constraintdef` / `pg_get_indexdef` /
    `pg_get_triggerdef` + `md5(thân hàm)`, thêm `column_default`, mặt khoá chính,
    mặt bảng+RLS. Bảy mặt.
  - **Tự bắt được lỗi nguy hiểm nhất của cả ticket TRƯỚC khi gửi review:** bản đầu
    để `MAT` chung file với mã sinh ở cấp cao nhất, nên chạy test là chạy trình
    sinh và **ghi đè chính fixture đang được kiểm** — test tự chứng nhận chính
    mình. Bắt được vì thấy log của trình sinh in giữa output test. Tách module thuần.
  - **R3 APPROVE.** Phép kiểm quyết định: 43 dòng khác + 10/54 đỏ, so với 0 dòng
    và 51/51 xanh vòng trước. R3 tìm thêm 11 phép, 9 lọt, nhưng phân loại tất cả
    là nợ theo **quy tắc dừng viết ra trước khi chấm**.
  - Hồ sơ review phải viết lại **ba lần** mới qua `review_check`: tiêu đề mục bằng
    chứng chứa "R3" nên bị cắt thành card riêng; đường dẫn cụt không phải file có
    thật; diff high-stakes đòi reviewer thứ ba **so sánh phương án**. Cổng đúng cả
    ba lần.
  - PR #4, CI xanh, self-merge. WMS-2 → Done.
  → chi tiết: evd/WMS-2/dev/review.md · docs/pm/decisions.md A9, A10
- Result: done — **WMS-3 và WMS-6 hết bị chặn.**

## Tổng kết 3 dòng

1. Sprint-1 tiêu **2,0 pd** (WMS-1 + WMS-2). Lần đầu một ticket có mã sản phẩm đi
   hết vòng: 3/3 chữ ký, CI xanh, merge, Done.
2. Hình dạng của 5 vòng review đáng giữ hơn con số: vòng 1 bắt lỗi trong sản phẩm,
   vòng 2–3 trong hàng rào, vòng 4–5 trong hàng rào của hàng rào. Reviewer là
   người viết ra rằng tầng tiếp theo luôn tồn tại — nên điểm dừng phải là quy tắc
   viết trước, không phải cảm giác đã đủ.
3. Lần đầu tôi tìm ra lỗi nghiêm trọng nhất của một vòng trước khi reviewer làm
   hộ. Cái dạy tôi cách nhìn ra nó chính là ba vòng bị đục thủng trước đó.
